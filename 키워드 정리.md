# 운영체제 / 프로그래밍 키워드 정리

## 해시코드

- 해시코드(Hash Code)는 데이터를 숫자 형태의 값으로 바꾼 결과값이다.
- 어떤 문자열이나 객체를 빠르게 찾기 위해 사용한다.
- 완전히 다른 데이터라도 같은 해시코드가 나올 수 있는데, 이걸 충돌(Collision)이라고 한다.
- 대표 사용 예시는 `Dictionary`, `HashSet`, 해시 테이블이다.

---

## CPU와 GPU의 차이

- CPU는 다양한 작업을 골고루 처리하는 범용 연산 장치다.
- GPU는 단순한 연산을 엄청 많이 동시에 처리하는 데 강한 장치다.
- CPU는 코어 수가 적지만 제어가 강하고 복잡한 분기 처리에 유리하다.
- GPU는 코어 수가 많아서 그래픽 처리, 병렬 처리에 유리하다.

### 쉽게 구분
- CPU = 소수의 똑똑한 일꾼
- GPU = 단순 작업을 동시에 처리하는 대량의 일꾼

---

## 소프트웨어 정의

- 소프트웨어는 하드웨어를 동작시키기 위한 프로그램, 데이터, 관련 문서의 집합이다.
- 눈에 보이는 기계 자체는 하드웨어이고, 그 기계를 어떻게 움직일지 적어둔 명령 묶음이 소프트웨어다.
- 예: 운영체제, 게임, 앱, 브라우저

---

## 소프트웨어 특성

- 눈에 보이는 물건이 아니다.
- 복제가 쉽다.
- 마모되지는 않지만 유지보수를 안 하면 품질이 떨어질 수 있다.
- 복잡성이 높다.
- 수정과 확장이 자주 일어난다.

---

## 소프트웨어 위기

- 소프트웨어가 점점 복잡해지면서 개발 일정 지연, 품질 저하, 유지보수 문제, 비용 증가가 심해진 현상을 말한다.
- 프로그램 규모가 커질수록 사람이 손으로만 관리하기 힘들어졌다.
- 이 문제를 줄이기 위해 소프트웨어 공학 개념이 발전했다.

---

## SDLC

- SDLC는 Software Development Life Cycle, 소프트웨어 개발 생명주기다.
- 소프트웨어를 만들 때 거치는 전체 과정이다.

### 일반적인 단계
- 요구사항 분석
- 설계
- 구현
- 테스트
- 배포
- 유지보수

---

## 다형성

- 다형성은 같은 이름의 기능이 상황에 따라 다르게 동작하는 성질이다.
- 객체지향의 핵심 개념 중 하나다.

### 예시
- 같은 `Attack()` 함수라도 캐릭터마다 다른 방식으로 실행될 수 있다.
- 부모 타입으로 자식 객체를 다룰 수 있다.

---

## 가상화

- 실제 물리 자원을 논리적으로 나눠서 여러 개처럼 사용하는 기술이다.
- 하나의 물리 컴퓨터에서 여러 운영체제를 돌릴 수도 있다.
- 하드웨어를 직접 쓰는 대신 가상의 환경을 만들어 사용하는 개념이다.

### 예시
- 가상 머신
- 가상 메모리
- 가상 서버

---

## 추상화

- 복잡한 내부 내용은 숨기고, 중요한 특징만 뽑아서 단순하게 표현하는 것이다.
- 사용자는 “어떻게 동작하는지”보다 “무엇을 하는지”를 중심으로 본다.

### 예시
- 자동차를 탈 때 엔진 내부 구조를 몰라도 운전 가능
- 코드에서는 인터페이스, 추상 클래스가 추상화와 연결된다

---

## CBD(Component Based Development)

- CBD는 Component Based Development, 컴포넌트 기반 개발이다.
- 재사용 가능한 부품(Component)을 조립해서 프로그램을 만드는 방식이다.
- 큰 프로그램을 작은 부품 단위로 나눠 개발하고, 필요하면 다시 조립해서 사용한다.

### 장점
- 재사용성 증가
- 유지보수 편리
- 개발 속도 향상

---

## 결합도

- 결합도는 모듈이나 클래스끼리 얼마나 강하게 의존하는지를 뜻한다.
- 결합도가 높으면 한쪽을 수정할 때 다른 쪽도 많이 영향을 받는다.
- 결합도는 낮을수록 좋다.

### 쉽게 말하면
- 서로 너무 꽉 묶여 있으면 고치기 힘들다.
- 적당히 분리되어 있어야 유지보수가 쉽다.

---

## 코드 스멜

- 코드 스멜(Code Smell)은 코드에서 “뭔가 구조가 안 좋다”는 냄새가 나는 징후다.
- 당장 오류는 아닐 수 있지만, 유지보수성과 가독성이 떨어질 가능성이 높다.

### 예시
- 너무 긴 함수
- 중복 코드
- 의미 없는 변수명
- 너무 많은 책임을 가진 클래스

---

## 리팩토링

- 리팩토링은 겉으로 보이는 기능은 그대로 두고 내부 구조를 더 좋게 고치는 작업이다.
- 코드 스멜을 줄이고 유지보수성을 높이기 위해 한다.

### 예시
- 긴 함수 나누기
- 중복 코드 제거
- 변수명, 함수명 더 명확하게 바꾸기

---

## Validation

- Validation은 “올바른 제품을 만들고 있는가?”를 보는 것이다.
- 사용자의 요구사항과 기대에 맞는지 확인하는 관점이다.

### 쉽게 말하면
- 만든 결과물이 진짜 사용자가 원한 게 맞는가?

---

## Verification

- Verification은 “제품을 올바르게 만들고 있는가?”를 보는 것이다.
- 설계서, 명세서, 요구사항을 정확히 지켜서 만들고 있는지를 확인한다.

### 쉽게 말하면
- 정해진 방식대로 제대로 만들고 있는가?

---

## 추상클래스

- 추상클래스는 직접 객체를 만들기 위한 클래스가 아니라, 공통 틀을 제공하기 위한 클래스다.
- 일부 메서드는 구현하고, 일부 메서드는 자식이 반드시 구현하도록 강제할 수 있다.

### 특징
- 공통 기능 제공 가능
- 상속을 전제로 사용
- 직접 `new`로 생성 불가

---

## 인터페이스

- 인터페이스는 “이 기능들을 반드시 구현해라”라고 약속하는 규칙이다.
- 내부 구현보다 기능의 형태를 맞추는 데 목적이 있다.

### 특징
- 다중 구현 가능
- 공통 규칙 정의
- 추상화에 자주 사용

---

## 유즈케이스 다이어그램

- 사용자가 시스템을 어떻게 사용하는지를 그림으로 나타낸 것이다.
- 사용자(Actor)와 시스템 기능(Use Case)의 관계를 보여준다.
- 시스템 외부에서 누가 어떤 기능을 쓰는지 이해할 때 좋다.

---

## 클래스 다이어그램

- 클래스, 속성, 메서드, 클래스 간 관계를 표현하는 UML 다이어그램이다.
- 설계 단계에서 구조를 보는 데 자주 사용한다.
- 상속, 연관, 집합 같은 관계도 표현 가능하다.

---

## 시퀀스 다이어그램

- 객체들 사이에서 메시지가 시간 순서대로 어떻게 오가는지 표현한 UML 다이어그램이다.
- “누가 먼저 호출하고, 누가 응답하는지” 흐름을 보는 데 좋다.

---

## .lib(Static Library)

- 정적 라이브러리다.
- 프로그램 빌드 시점에 실행 파일에 같이 묶인다.
- 실행할 때 별도 파일이 없어도 된다.

### 특징
- 배포가 단순함
- 실행 파일 크기가 커질 수 있음
- 수정 시 다시 빌드 필요

---

## DLL

- DLL은 Dynamic Link Library, 동적 라이브러리다.
- 프로그램 실행 중에 연결해서 사용하는 라이브러리다.
- 실행 파일과 분리되어 있다.

### 특징
- 코드 재사용 가능
- 파일 교체로 업데이트 가능
- DLL이 없으면 실행 오류가 날 수 있음

---

## PC(Program Counter)

- 다음에 실행할 명령어의 주소를 저장하는 레지스터다.
- CPU는 PC를 보고 다음 명령어를 어디서 가져올지 결정한다.

---

## IR(Instruction Register)

- 현재 실행 중인 명령어를 저장하는 레지스터다.
- 메모리에서 가져온 명령어를 일단 IR에 넣고 해석한다.

---

## AC(Accumulator)

- 연산 결과를 임시로 저장하는 레지스터다.
- 예전 구조의 CPU 설명에서 많이 나온다.
- ALU 연산 결과를 잠깐 담는 용도로 이해하면 된다.

---

## MAR(Memory Address Register)

- 접근할 메모리 주소를 저장하는 레지스터다.
- CPU가 메모리에 읽기/쓰기를 할 때 “어디를 볼지” 저장한다.

---

## MBR(Memory Buffer Register)

- 메모리와 주고받는 실제 데이터를 잠깐 저장하는 레지스터다.
- 메모리에서 읽어온 값이나 메모리에 쓸 값을 담는다.

---

## ALU

- ALU는 Arithmetic Logic Unit, 산술논리연산장치다.
- 덧셈, 뺄셈, 비교, 논리연산 같은 실제 계산을 담당한다.

---

## Decoder

- 디코더는 명령어를 해석해서 CPU 내부에 어떤 동작을 시킬지 결정하는 장치다.
- “이 명령은 덧셈이네”, “이 명령은 메모리 읽기네” 같은 해석을 담당한다.

---

## Shifter

- 비트를 왼쪽/오른쪽으로 이동시키는 장치다.
- 곱셈, 나눗셈, 비트 연산과 관련해서 자주 사용된다.

---

## Address Bus

- 주소를 전달하는 버스다.
- CPU가 메모리나 장치에 “몇 번지에 접근할지”를 보낼 때 사용한다.

---

## Data Bus

- 실제 데이터를 전달하는 버스다.
- CPU와 메모리, 입출력 장치 사이에서 값이 오갈 때 사용한다.

---

## Control Bus

- 제어 신호를 전달하는 버스다.
- 읽기, 쓰기, 인터럽트 같은 제어 정보를 주고받는다.

---

## 명령어 사이클

- CPU가 명령어 하나를 처리하는 전체 과정이다.
- 보통 인출 → 해독 → 실행의 흐름으로 본다.
- 경우에 따라 간접 사이클, 인터럽트 사이클도 포함된다.

---

## 인출 사이클

- 메모리에서 명령어를 가져오는 단계다.
- PC가 가리키는 주소에서 명령어를 읽어온다.

---

## 간접 사이클

- 명령어가 실제 피연산자 주소를 바로 가지지 않고, 주소가 저장된 위치를 한 번 더 참조해야 할 때 수행되는 단계다.
- 한 번 더 메모리를 들러야 해서 추가 과정이 생긴다.

---

## 실행 사이클

- 해석이 끝난 명령어를 실제로 수행하는 단계다.
- 덧셈, 이동, 저장, 점프 같은 동작이 여기서 일어난다.

---

## 인터럽트 사이클

- CPU가 현재 하던 일을 잠깐 멈추고, 긴급한 요청을 처리하는 단계다.
- 처리 후에는 다시 원래 작업으로 복귀할 수 있다.

---

## 캐시 메모리

- CPU와 메인 메모리 사이에 있는 빠른 임시 저장소다.
- 자주 쓰는 데이터를 가까운 곳에 둬서 속도를 높인다.
- RAM보다 작지만 훨씬 빠르다.

---

## 캐시 메모리 지역성

- 프로그램은 특정 데이터나 명령어를 몰아서 쓰는 경향이 있다.
- 이 성질을 지역성(Locality)이라고 한다.

### 종류
- 시간 지역성: 최근 쓴 것을 다시 쓰는 경향
- 공간 지역성: 근처 주소를 같이 쓰는 경향

---

## 캐시메모리 일관성

- 여러 캐시가 같은 메모리 값을 들고 있을 때 값이 서로 다르면 문제가 생긴다.
- 이 값을 서로 맞추는 문제를 캐시 일관성이라고 한다.
- 멀티코어 환경에서 특히 중요하다.

---

## 캐시메모리 매핑

- 메인 메모리의 데이터를 캐시에 어떤 방식으로 배치할지 정하는 방법이다.

### 종류
- 직접 매핑
- 연관 매핑
- 집합 연관 매핑

---

## 가상메모리

- 실제 RAM보다 더 큰 메모리를 쓰는 것처럼 보이게 하는 기술이다.
- 보조기억장치 일부를 메모리처럼 사용한다.
- 프로그램마다 자기만의 큰 메모리 공간을 가진 것처럼 보이게 해준다.

---

## 가상메모리 관리정책

- 어떤 페이지를 메모리에 올릴지
- 어떤 페이지를 내릴지
- 언제 교체할지  
이런 규칙을 관리하는 방식이다.

### 예시
- 페이지 교체 정책
- 할당 정책
- 적재 정책

---

## 가상메모리 페이징

- 메모리를 같은 크기의 페이지 단위로 나눠 관리하는 방식이다.
- 외부 단편화를 줄이기 좋다.
- 주소 변환을 위해 페이지 테이블이 필요하다.

---

## 가상메모리 세그먼테이션

- 프로그램을 의미 단위로 나눠서 관리하는 방식이다.
- 코드, 데이터, 스택처럼 논리적 단위로 구분한다.
- 세그먼트마다 크기가 다를 수 있다.

---

## 가상메모리 페이지드 세그멘테이션

- 세그먼테이션과 페이징을 합친 방식이다.
- 논리적으로는 세그먼트로 나누고, 실제 관리에서는 페이지 단위로 처리한다.

---

## Thrashing

- 스래싱은 페이지 교체가 너무 자주 일어나서 CPU가 일보다 메모리 교체에 더 많은 시간을 쓰는 상태다.
- 프로그램은 거의 일을 못 하고 계속 페이지 넣고 빼기만 한다.
- 성능이 급격히 떨어진다.

---

## 메모리 인터리빙

- 메모리를 여러 뱅크로 나눠서 번갈아 접근하게 하여 속도를 높이는 방식이다.
- 동시에 더 효율적으로 데이터를 공급할 수 있다.

---

## 메모리 단편화

- 메모리를 사용하고 해제하는 과정에서 공간이 쪼개지는 현상이다.
- 필요한 메모리가 충분히 남아 있어도, 조각나 있으면 활용이 어렵다.

---

## 메모리 단편화 - 내부

- 할당한 메모리 블록 안에서 남는 공간이 생기는 현상이다.
- 예를 들어 10칸이 필요한데 16칸을 통째로 할당받으면 안쪽에 남는 공간이 생긴다.

---

## 메모리 단편화 - 외부

- 빈 메모리는 충분한데 여기저기 흩어져 있어서 큰 덩어리로 못 쓰는 현상이다.
- 메모리 사이사이가 조각난 상태라고 보면 된다.

---

## 메모리 단편화 - 압축

- 흩어진 메모리 조각을 한쪽으로 몰아서 큰 빈 공간을 만드는 방식이다.
- 외부 단편화 해결 방법 중 하나다.
- 다만 이동 비용이 든다.

---

## 메모리 단편화 - 메모리 풀

- 자주 쓰는 크기의 메모리를 미리 여러 개 만들어두고 재사용하는 방식이다.
- 할당/해제가 반복되는 상황에서 단편화를 줄이고 속도를 높이는 데 도움 된다.
- 게임 개발에서도 많이 쓴다.

---

## 가비지 컬렉션

- 더 이상 사용하지 않는 메모리를 자동으로 정리하는 기능이다.
- C#, Java 같은 언어에서 대표적이다.
- 개발자가 직접 `free`하지 않아도 되지만, 언제 정리될지 완전히 마음대로 조절하긴 어렵다.

---

## 컴파일러

- 전체 소스코드를 한 번에 번역해서 기계어나 실행 파일로 바꾸는 프로그램이다.
- 실행 전에 번역을 끝내는 방식이다.

### 특징
- 실행 속도 빠름
- 번역 단계 필요
- 오류를 미리 많이 발견 가능

---

## 인터프리터

- 코드를 한 줄씩 읽고 바로 실행하는 방식이다.
- 실행하면서 해석한다.

### 특징
- 실행 전 전체 번역이 필요 없음
- 디버깅이 편한 경우가 있음
- 일반적으로 컴파일 방식보다 느릴 수 있음

---

## 컴파일러 vs 인터프리터

- 컴파일러는 “미리 다 번역하고 실행”
- 인터프리터는 “읽으면서 바로 실행”

### 비유
- 컴파일러 = 시험 전에 번역본을 다 만들어두는 것
- 인터프리터 = 읽으면서 바로바로 통역하는 것

---

## GPU

- GPU는 Graphics Processing Unit, 그래픽 처리 장치다.
- 원래 목적은 화면에 보이는 3D/2D 그래픽을 빠르게 계산해서 출력하는 것이었다.
- 하지만 구조상 “같은 계산을 아주 많이 반복하는 일”에 강해서, 지금은 그래픽뿐 아니라 연산용으로도 많이 쓰인다.

### 왜 GPU가 따로 필요할까?

- 게임에서 화면을 그릴 때는 한두 개 계산이 아니라, 엄청 많은 픽셀과 정점(Vertex), 텍스처 계산이 동시에 일어난다.
- 이런 작업을 CPU 혼자 하면 너무 무겁다.
- 그래서 화면 계산 전용으로 훨씬 병렬 처리에 강한 GPU가 쓰인다.



### 게임에서 GPU가 많이 하는 일

- 캐릭터, 배경, 이펙트 렌더링
- 조명 계산
- 그림자 처리
- 후처리 효과
- 화면 해상도에 따른 픽셀 계산

---

## GPGPU

- GPGPU는 General Purpose computing on GPU의 줄임말이다.
- 뜻은 “GPU를 그래픽 말고 일반 계산에도 사용한다”는 의미다.
- 원래 GPU는 그림을 그리는 용도였지만, 계산 능력이 너무 강해서 과학 계산, AI, 시뮬레이션에도 쓰이게 되었다.

### 왜 쓰는가?

- GPU는 같은 계산을 수천 개 단위로 동시에 처리하는 데 강하다.
- 그래서 행렬 계산, 물리 시뮬레이션, 이미지 처리, 딥러닝처럼 반복 계산이 많은 작업에서 엄청 빠르다.

### 예시

- 딥러닝 학습
- 영상 필터 처리
- 대규모 물리 시뮬레이션
- 입자(Particle) 계산

---

## CPU vs GPU

- CPU와 GPU는 둘 다 연산 장치지만, 잘하는 일이 다르다.
- CPU는 다양한 종류의 일을 유연하게 처리하는 데 강하고, GPU는 똑같은 계산을 대량으로 동시에 처리하는 데 강하다.

| 항목 | CPU | GPU |
|------|-----|-----|
| 주 역할 | 범용 연산, 제어 | 대량 병렬 연산 |
| 코어 수 | 적음 | 많음 |
| 강점 | 복잡한 판단, 분기, 제어 | 반복 계산, 병렬 처리 |
| 예시 | 게임 로직, 입력 처리, 운영체제 작업 | 렌더링, 셰이더, 대규모 계산 |

### 게임 기준으로 보면

- CPU는 플레이어 입력, AI 판단, 게임 규칙, 씬 관리 같은 걸 많이 담당한다.
- GPU는 “결국 화면에 무엇을 어떻게 보여줄지”를 빠르게 계산한다.



---

## CUDA

- CUDA는 NVIDIA가 만든 GPU 프로그래밍 플랫폼이다.
- 개발자가 GPU를 그래픽 출력뿐 아니라 일반 연산에도 활용할 수 있게 해준다.
- 즉, GPU에게 “그림만 그리지 말고 계산도 해”라고 시키기 쉽게 만든 도구라고 보면 된다.

### 특징

- NVIDIA 그래픽카드 기반
- 병렬 연산에 특화
- AI, 과학 계산, 시뮬레이션 분야에서 많이 사용

### 주의

- CUDA는 보통 NVIDIA 환경과 강하게 연결된다.
- 그래서 “GPU 연산” 전체를 CUDA라고 생각하면 안 되고, CUDA는 그중 한 플랫폼이다.

---

## RAM vs VRAM

- RAM은 일반 시스템 메모리다.
- VRAM은 비디오 메모리, 즉 GPU가 그래픽 처리를 위해 주로 사용하는 메모리다.

### RAM

- CPU가 주로 사용한다.
- 프로그램 실행, 데이터 저장, 운영체제 동작에 쓰인다.

### VRAM

- GPU가 주로 사용한다.
- 텍스처, 프레임 버퍼, 셰이더 데이터, 모델 데이터 등을 저장한다.

### 차이 느낌

- RAM = 게임 전체 운영에 필요한 작업 공간
- VRAM = 화면을 실제로 그리는 데 필요한 전용 작업 공간

### 게임에서 중요한 이유

- 고해상도 텍스처, 큰 모델, 많은 이펙트를 쓰면 VRAM 사용량이 커진다.
- VRAM이 부족하면 끊김, 로딩 지연, 프레임 저하가 생길 수 있다.

---

## Compute Shader

- Compute Shader는 GPU로 일반 계산을 수행하기 위한 셰이더다.
- 원래 셰이더는 화면을 그리는 과정에서 쓰였는데, Compute Shader는 “화면용”이 아니라 “계산용”에 더 가깝다.

### 어디에 쓰는가?

- 물리 계산
- 군중/입자 처리
- 이미지 후처리
- 대량 데이터 병렬 계산

### 왜 좋은가?

- 같은 계산을 많은 데이터에 동시에 적용할 수 있다.
- CPU보다 훨씬 빠르게 끝나는 경우가 많다.

### 게임 개발에서 느낌

- 몬스터 떼, 비, 눈, 입자 같은 걸 CPU가 다 계산하면 무거운데, 일부를 GPU 계산으로 넘기면 효율이 좋아질 수 있다.

---

## 프로세스

- 프로세스(Process)는 실행 중인 프로그램이다.
- 단순히 저장된 `.exe` 파일이나 앱 파일이 아니라, 메모리에 올라와 실제로 돌아가고 있는 상태를 말한다.

### 예시

- 크롬을 실행하면 크롬 프로세스가 생긴다.
- 게임을 실행하면 게임 프로세스가 생긴다.

### 중요한 점

- 프로그램 = 디스크에 저장된 파일
- 프로세스 = 메모리에 올라와 실행 중인 프로그램

### 프로세스가 가지는 것

- 코드
- 데이터
- 메모리 공간
- 실행 상태
- 운영체제가 관리하기 위한 정보

---

## 프로세스 메모리 영역

- 프로세스는 메모리 안에서 한 덩어리로만 있는 게 아니라, 역할별로 나눠서 관리된다.
- 대표적으로 코드 영역, 데이터 영역, 힙 영역, 스택 영역으로 구분한다.

### 왜 나누는가?

- 각 데이터의 성격이 다르기 때문이다.
- 프로그램 코드, 전역 변수, 동적 생성 데이터, 함수 호출 데이터는 관리 방식이 다르다.

### 쉽게 비유하면

- 코드 영역 = 설명서 보관함
- 데이터 영역 = 오래 유지되는 공용 창고
- 힙 영역 = 필요할 때 빌려 쓰는 창고
- 스택 영역 = 함수 호출할 때 잠깐 쓰는 작업대

---

## 코드 영역

- 코드 영역(Text Segment)은 실행할 명령어가 저장되는 공간이다.
- 우리가 작성한 프로그램의 기계어 명령들이 올라가는 곳이다.
- 보통 실행 중 자주 바뀌지 않으며 읽기 전용 성격이 강하다.

### 특징

- 프로그램 동작 명령 저장
- 보통 수정되지 않음
- 여러 실행 흐름이 같은 코드를 공유 가능

---

## 데이터 영역

- 데이터 영역은 프로그램이 시작할 때부터 끝날 때까지 유지되는 데이터가 저장되는 영역이다.
- 주로 전역 변수(static 포함)가 들어간다.

### 예시

- 전역 변수
- static 변수

### 특징

- 프로그램 시작 시 할당
- 프로그램 종료 시 해제
- 함수가 끝나도 사라지지 않음

---

## 힙 영역

- 힙 영역은 실행 중에 동적으로 메모리를 할당받는 공간이다.
- 필요할 때 만들고, 더 이상 필요 없으면 해제한다.

### 예시

- C에서 `malloc`
- C++에서 `new`
- C#에서 `new`로 만든 참조형 객체들

### 특징

- 런타임 중 크기 결정 가능
- 유연하지만 관리 비용이 있음
- 잘못 쓰면 메모리 누수나 단편화 문제가 생길 수 있음

### 쉽게 말하면

- “필요할 때 창고를 빌려 쓰는 공간”

---

## 스택 영역

- 스택 영역은 함수 호출 시 필요한 정보가 쌓이는 공간이다.
- 지역 변수, 매개변수, 반환 주소 등이 저장된다.

### 특징

- 함수 호출 시 쌓임
- 함수 종료 시 자동 제거
- 매우 빠름
- 구조가 단순함

### 왜 빠를까?

- 스택은 보통 맨 위에 쌓고 맨 위에서 꺼내는 LIFO 구조라서 관리가 단순하다.
- 운영체제가 복잡하게 빈 공간을 찾을 필요가 적다.



---

## 파일 포맷

- 파일 포맷은 데이터를 어떤 구조와 규칙으로 저장할지를 정한 형식이다.
- 확장자가 같아 보여도 내부 저장 방식과 목적이 다를 수 있다.

### 왜 중요할까?

- 같은 이미지라도 JPG, PNG, BMP는 압축 방식과 용도가 다르다.
- 같은 소리나 영상도 포맷에 따라 용량, 품질, 호환성이 다르다.

---

## Jpg

- JPG는 사진 저장에 많이 쓰이는 이미지 포맷이다.
- 손실 압축 방식을 사용해서 용량을 크게 줄일 수 있다.
- 대신 압축 과정에서 일부 화질 손실이 생길 수 있다.

### 장점
- 용량이 작아지기 쉽다.
- 사진 저장에 적합하다.

### 단점
- 저장할수록 품질 손실이 누적될 수 있다.
- 투명도 지원이 약하다.

---

## Bmp

- BMP는 비트맵 이미지 포맷이다.
- 구조가 단순하고 압축이 적어서 용량이 큰 편이다.

### 특징
- 품질 손실이 거의 없다.
- 파일 크기가 크다.
- 예전 윈도우 환경에서 많이 보였다.

---

## Tga

- TGA는 게임 그래픽 작업에서 예전부터 자주 쓰이던 이미지 포맷이다.
- 알파 채널(투명도) 처리와 텍스처 작업에서 편하게 쓰이는 경우가 많다.

### 특징
- 게임 리소스 작업에서 자주 등장
- 투명 정보 처리 가능
- 용량은 PNG보다 큰 편일 수 있음

---

## PNG

- PNG는 무손실 압축 이미지 포맷이다.
- 화질 손실 없이 저장할 수 있고, 투명도 지원이 좋다.

### 장점
- 아이콘, UI, 스프라이트에 적합
- 투명 배경 사용 가능
- 품질 유지에 유리

### 단점
- 사진 저장 시 JPG보다 용량이 커질 수 있음

---

## Wav

- WAV는 대표적인 오디오 파일 포맷이다.
- 비압축 또는 저압축 형태가 많아 음질이 좋다.
- 대신 파일 용량이 큰 편이다.

### 게임에서 자주 쓰는 이유

- 효과음은 짧고 즉시 재생이 중요해서 WAV가 편한 경우가 있다.
- 다만 프로젝트에 따라 mp3, ogg도 함께 쓴다.

---

## Avi

- AVI는 영상과 음성을 담을 수 있는 비디오 포맷이다.
- 오래된 형식이지만 기본 개념 설명에서 자주 등장한다.

### 특징
- 컨테이너 형식으로 이해하는 경우가 많다.
- 압축 방식에 따라 품질과 용량이 달라질 수 있다.
- 요즘은 mp4 같은 형식이 더 자주 보인다.

---

## Thread

- 스레드(Thread)는 프로세스 안에서 실제 실행 흐름을 담당하는 단위다.
- 한 프로세스 안에는 하나 이상의 스레드가 있을 수 있다.

### 예시

- 게임 메인 루프 실행
- 백그라운드 로딩
- 네트워크 처리

### 핵심

- 프로세스는 “큰 작업 단위”
- 스레드는 “그 안에서 실제로 움직이는 일꾼”

---

## Process

- Process도 프로세스를 뜻한다.
- 저장된 프로그램이 실제 실행 상태가 되면 프로세스가 된다.
- 운영체제는 프로세스를 단위로 메모리와 자원을 관리한다.

---

## Process vs Thread

- 프로세스는 독립된 실행 단위이고, 스레드는 프로세스 내부의 실행 흐름이다.
- 프로세스는 메모리 공간이 분리되어 있고, 스레드는 같은 프로세스 안에서 자원을 공유한다.

| 항목 | 프로세스 | 스레드 |
|------|----------|--------|
| 의미 | 실행 중인 프로그램 | 프로세스 내부 실행 흐름 |
| 메모리 | 독립적 | 같은 프로세스 내 공유 |
| 생성 비용 | 큼 | 상대적으로 작음 |
| 통신 | 복잡함 | 공유 자원 사용 가능 |
| 위험 | 독립적이라 영향 분리 쉬움 | 잘못 건드리면 동기화 문제 발생 |

### 주의

- 스레드는 메모리를 공유하니까 빠르고 편할 수 있다.
- 대신 동시에 같은 데이터에 접근하면 충돌 문제가 생길 수 있다.

---

## PCB(Process Control Block)

- PCB는 Process Control Block, 프로세스 제어 블록이다.
- 운영체제가 각 프로세스를 관리하기 위해 저장해두는 정보 묶음이다.
- 프로세스의 “관리 카드” 같은 느낌으로 이해하면 좋다.

### 들어가는 정보 예시
- Process ID
- 프로세스 상태
- 프로그램 카운터
- CPU 레지스터 정보
- 스케줄링 정보
- 메모리 관련 정보

---

## TCB(Thread Control Block)

- TCB는 Thread Control Block, 스레드 제어 블록이다.
- 운영체제가 각 스레드를 관리하기 위한 정보 묶음이다.
- PCB가 프로세스 전체 관리 카드라면, TCB는 스레드 개별 관리 카드다.

### 들어가는 정보 예시
- Thread ID
- 스레드 상태
- PC
- 레지스터 정보
- 스케줄링 관련 정보

---

## PCB - Process id

- Process ID는 각 프로세스를 식별하기 위한 고유 번호다.
- 운영체제가 여러 프로세스를 구분할 때 사용한다.
- 사람 이름이 아니라 주민등록번호처럼 “겹치지 않는 식별자”라고 생각하면 쉽다.

---

## TCB - Thread Id

- Thread ID는 각 스레드를 식별하기 위한 고유 번호다.
- 한 프로세스 안에 여러 스레드가 있어도 각각을 따로 구분할 수 있다.

---

## PCB - Process Status

- 프로세스 상태 정보다.
- 현재 이 프로세스가 어떤 상태에 있는지 운영체제가 기록한다.

### 대표 상태
- New: 생성됨
- Ready: 실행 준비 완료
- Running: 실행 중
- Waiting: 대기 중
- Terminated: 종료됨

---

## TCB - Thread Status

- 스레드 상태 정보다.
- 현재 스레드가 실행 중인지, 대기 중인지, 준비 상태인지 등을 기록한다.
- 프로세스와 비슷하지만, 더 작은 실행 단위를 관리하는 느낌이다.

---

## CPU register

- CPU 레지스터는 CPU 내부에 있는 아주 빠른 저장 공간이다.
- 연산 중인 값, 주소, 상태 정보를 잠깐 저장한다.
- RAM보다 훨씬 빠르며, CPU가 즉시 접근해야 하는 값들을 담는다.

### 왜 중요한가?

- CPU는 계산할 때 계속 메모리만 왔다 갔다 하면 느리다.
- 그래서 지금 당장 필요한 값을 레지스터에 넣고 빠르게 처리한다.


---

## CPU Register - PC(Program Counter)

- PC는 다음에 실행할 명령어의 주소를 저장하는 레지스터다.
- CPU는 PC를 보고 “다음에 어디 코드로 갈지” 결정한다.

### 중요 포인트
- 순차 실행이면 다음 주소로 증가
- 점프/분기 명령이 있으면 다른 주소로 바뀜
- 프로그램 흐름 제어의 핵심

---

## PCB - PC

- 프로세스가 실행을 멈췄다가 다시 이어서 실행될 수 있으려면, 어디까지 했는지 기억해야 한다.
- 그때 프로세스의 PC 값이 PCB에 저장된다.
- 즉, 프로세스 문맥 전환(Context Switch) 시 복귀 위치를 기록해두는 것이다.

---

## TCB - PC

- 스레드도 마찬가지로, 중간에 멈췄다가 다시 이어서 실행할 수 있어야 한다.
- 그래서 스레드의 PC 값은 TCB에 저장된다.
- 멀티스레드 환경에서 특히 중요하다.

---

## CPU Register - IR

- IR은 현재 실행 중인 명령어를 저장하는 레지스터다.
- 메모리에서 명령어를 가져오면 CPU는 그걸 IR에 넣고 해석한다.

### 흐름
- 메모리에서 명령어 읽기
- IR에 저장
- 제어장치가 해석
- 필요한 동작 수행

---

## CPU Register - MAR

- MAR은 Memory Address Register다.
- CPU가 접근하려는 메모리 주소를 저장한다.
- 쉽게 말해 “어디 메모리를 볼지”를 적어두는 칸이다.

---

## CPU Register - MBR

- MBR은 Memory Buffer Register다.
- 메모리와 주고받는 실제 데이터를 잠깐 저장하는 레지스터다.
- 읽어온 값이나 저장할 값을 담는다.

### MAR와 MBR 차이
- MAR = 주소
- MBR = 데이터

---

## CPU Register - GPR

- GPR은 General Purpose Register, 범용 레지스터다.
- 특정 한 가지 용도만 하는 게 아니라, 여러 일반 연산에 폭넓게 사용된다.
- 계산 중간값 저장, 임시 값 저장 등에 많이 사용한다.

---

## CPU Register - AC

- AC는 Accumulator, 누산기다.
- 연산 결과를 임시로 저장하는 데 사용되는 레지스터다.
- 예전 CPU 구조 설명에서 자주 등장하며, 기본 연산 구조를 이해할 때 중요하다.

### 느낌
- 계산기 화면에 지금 결과가 잠깐 떠 있는 칸 같은 느낌

---

## CPU Register(Flag Register)

- Flag Register는 연산 결과의 상태를 저장하는 레지스터다.
- 계산 결과가 0인지, 음수인지, 자리올림이 있었는지 같은 정보를 기록한다.

### 왜 필요할까?

- 조건 분기에서 사용된다.
- 예를 들어 “결과가 0이면 점프” 같은 판단을 할 때 이 플래그 값을 본다.

### 예시 상태
- Zero Flag
- Sign Flag
- Carry Flag
- Overflow Flag

---

## CPU Register - SP(Stack Pointer)

- SP는 Stack Pointer다.
- 현재 스택의 맨 위 위치를 가리키는 레지스터다.
- 함수 호출, 반환, 지역 변수 관리에 매우 중요하다.

### 왜 중요할까?

- 함수가 호출되면 스택에 값이 쌓인다.
- 함수가 끝나면 스택에서 값이 빠진다.
- 이때 SP가 “지금 스택의 어디까지 사용 중인지” 알려준다.

---

## CPU - 제어장치

- 제어장치는 CPU 내부에서 전체 동작 순서를 통제하는 장치다.
- 메모리에서 명령어를 가져오고, 그 명령어를 해석하고, 각 장치에 어떤 동작을 할지 제어 신호를 보낸다.

### 무슨 역할을 하나?

- 명령어 해석
- 실행 순서 제어
- ALU, 레지스터, 메모리 사이 동작 조정
- 읽기/쓰기 같은 제어 신호 전달

### 쉽게 비유하면

- ALU가 계산 담당 전사라면,
- 제어장치는 “지금 누구를 움직일지 지시하는 지휘관”이다.

### CPU 안 역할 분담 느낌
- 제어장치 = 지시
- ALU = 계산
- 레지스터 = 초고속 임시 저장소

### 흐름 예시
1. PC가 다음 명령어 위치를 가리킨다.
2. 명령어를 메모리에서 가져온다.
3. IR에 저장한다.
4. 제어장치가 해석한다.
5. ALU나 메모리, 레지스터에 필요한 동작을 지시한다.
6. 결과를 저장하고 다음 명령으로 넘어간다.