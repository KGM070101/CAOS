# 운영체제 / 프로그래밍 키워드 정리

## 해시코드

- 해시코드(Hash Code)는 데이터를 숫자 형태의 값으로 바꾼 결과값이다.(숫자에서 데이터로 복구 불가능)
- 어떤 문자열이나 객체를 빠르게 찾기 위해 사용한다.
- 완전히 다른 데이터라도 같은 해시코드가 나올 수 있는데, 이걸 충돌(Collision)이라고 한다.
- 대표 사용 예시는 `Dictionary`, `HashSet`, 해시 테이블이다.
- Unity 에셋 찾기에도 사용할 수 있다.

예시

![해시코드](<./Images/해시코드 예시.png>)

Instance ID가 해시코드이다.
데이터나 에셋마다 고유의 해시코드가 알고리즘을 통해 정해진다.

---

## CPU와 GPU의 차이

- CPU는 다양한 작업을 골고루 처리하는 범용 연산 장치다.
- GPU는 단순한 연산을 엄청 많이 동시에 처리하는 데 강한 장치다.
- CPU는 코어 수가 적지만 제어가 강하고 복잡한 분기 처리에 유리하다.
- GPU는 코어 수가 많아서 그래픽 처리, 병렬 처리에 유리하다.

### 쉽게 구분
- CPU = 소수의 똑똑한 일꾼
- GPU = 단순 작업을 동시에 처리하는 대량의 일꾼

---

## 소프트웨어 정의

- 소프트웨어는 하드웨어를 동작시키기 위한 프로그램, 데이터, 관련 문서의 집합이다.
- 눈에 보이는 기계 자체는 하드웨어이고, 그 기계를 어떻게 움직일지 적어둔 명령 묶음이 소프트웨어다.
- 예: 운영체제, 게임, 앱, 브라우저

---

## 소프트웨어 특성

- 눈에 보이는 물건이 아니다.
- 복제가 쉽다.
- 마모되지는 않지만 유지보수를 안 하면 품질이 떨어질 수 있다.
- 복잡성이 높다.
- 수정과 확장이 자주 일어난다.

---

## 소프트웨어 위기

- 소프트웨어가 점점 복잡해지면서 개발 일정 지연, 품질 저하, 유지보수 문제, 비용 증가가 심해진 현상을 말한다.
- 프로그램 규모가 커질수록 사람이 손으로만 관리하기 힘들어졌다.
- 이 문제를 줄이기 위해 소프트웨어 공학 개념이 발전했다.

---

## SDLC

- SDLC는 Software Development Life Cycle, 소프트웨어 개발 생명주기다.
- 소프트웨어를 만들 때 거치는 전체 과정이다.

### 일반적인 단계
- 요구사항 분석
- 설계
- 구현
- 테스트
- 배포
- 유지보수

---

## 다형성

- 다형성은 같은 이름의 기능이 상황에 따라 다르게 동작하는 성질이다.
- 객체지향의 핵심 개념 중 하나다.

### 예시
- 같은 `Attack()` 함수라도 캐릭터마다 다른 방식으로 실행될 수 있다.
- 부모 타입으로 자식 객체를 다룰 수 있다.

---

## 가상화

- 실제 물리 자원을 논리적으로 나눠서 여러 개처럼 사용하는 기술이다.
- 하나의 물리 컴퓨터에서 여러 운영체제를 돌릴 수도 있다.
- 하드웨어를 직접 쓰는 대신 가상의 환경을 만들어 사용하는 개념이다.

### 예시
- 가상 머신
- 가상 메모리
- 가상 서버

---

## 추상화

- 복잡한 내부 내용은 숨기고, 중요한 특징만 뽑아서 단순하게 표현하는 것이다.
- 사용자는 “어떻게 동작하는지”보다 “무엇을 하는지”를 중심으로 본다.

### 예시
- 자동차를 탈 때 엔진 내부 구조를 몰라도 운전 가능
- 코드에서는 인터페이스, 추상 클래스가 추상화와 연결된다

---

## CBD(Component Based Development)

- CBD는 Component Based Development, 컴포넌트 기반 개발이다.
- 재사용 가능한 부품(Component)을 조립해서 프로그램을 만드는 방식이다.
- 큰 프로그램을 작은 부품 단위로 나눠 개발하고, 필요하면 다시 조립해서 사용한다.

### 장점
- 재사용성 증가
- 유지보수 편리
- 개발 속도 향상

---

## 결합도

- 결합도는 모듈이나 클래스끼리 얼마나 강하게 의존하는지를 뜻한다.
- 결합도가 높으면 한쪽을 수정할 때 다른 쪽도 많이 영향을 받는다.
- 결합도는 낮을수록 좋다.

### 쉽게 말하면
- 서로 너무 꽉 묶여 있으면 고치기 힘들다.
- 적당히 분리되어 있어야 유지보수가 쉽다.

---

## 코드 스멜

- 코드 스멜(Code Smell)은 코드에서 “뭔가 구조가 안 좋다”는 냄새가 나는 징후다.
- 당장 오류는 아닐 수 있지만, 유지보수성과 가독성이 떨어질 가능성이 높다.

### 예시
- 너무 긴 함수
- 중복 코드
- 의미 없는 변수명
- 너무 많은 책임을 가진 클래스

---

## 리팩토링

- 리팩토링은 겉으로 보이는 기능은 그대로 두고 내부 구조를 더 좋게 고치는 작업이다.
- 코드 스멜을 줄이고 유지보수성을 높이기 위해 한다.

### 예시
- 긴 함수 나누기
- 중복 코드 제거
- 변수명, 함수명 더 명확하게 바꾸기

---

## Validation

- Validation은 “올바른 제품을 만들고 있는가?”를 보는 것이다.
- 사용자의 요구사항과 기대에 맞는지 확인하는 관점이다.

### 쉽게 말하면
- 만든 결과물이 진짜 사용자가 원한 게 맞는가?

---

## Verification

- Verification은 “제품을 올바르게 만들고 있는가?”를 보는 것이다.
- 설계서, 명세서, 요구사항을 정확히 지켜서 만들고 있는지를 확인한다.

### 쉽게 말하면
- 정해진 방식대로 제대로 만들고 있는가?

---

## 추상클래스

- 추상클래스는 직접 객체를 만들기 위한 클래스가 아니라, 공통 틀을 제공하기 위한 클래스다.
- 일부 메서드는 구현하고, 일부 메서드는 자식이 반드시 구현하도록 강제할 수 있다.
- 부모가 뼈대를 제공하고 자식이 세부동작을 완성한다.
- `abstract`키워드로 선언한다.

```csharp
abstract class Animal
{
    public abstract void Cry();

    public void Eat()
    {
        Console.WriteLine("먹는다");
    }
}

class Dog : Animal
{
    public override void Cry()
    {
        Console.WriteLine("멍멍");
    }
}
```
- `Animal`은 추상 클래스라서 직접 `new Animal()`을 못함.
- `Cry()`는 추상 메서드라서 Dog가 직접 구현 해야함.
- `Eat()`은 일반 메서드라서 부모쪽에서 구현 가능.
- 부모 클래스에 있던 메서드를 재구현 하므로 `override` 사용




### 특징
- 공통 기능 제공 가능
- 상속을 전제로 사용
- 직접 `new`로 생성 불가

---

## 인터페이스

- 인터페이스는 “이 기능들을 반드시 구현해라”라고 약속하는 규칙이다.
- 내부 구현보다 기능의 형태를 맞추는 데 목적이 있다.
- `interface`키워드로 선언한다.

```csharp
interface IMovable
{
    void Move();
}

class Car : IMovable
{
    public void Move()
    {
        Console.WriteLine("자동차가 움직인다");
    }
}
```
- `IMovable`은 `Move()`를 반드시 가져야 한다는 약속임.
- `Car`는 `Move()`를 반드시 가지고 직접 구현 해야함.

### 특징
- 다중 구현 가능
- 공통 규칙 정의
- 추상화에 자주 사용

---

## UML(Unified Modeling Language)

- 소프트웨어 시스템을 그림과 기호로 설계하고 설명하는 표준 언어이다.
- 프로그래밍 언어가 아니라, 시스템 구조나 동작을 시각화, 명세화, 문서화하기 위한 모델링 언어이다.
- 복잡한 시스템을 사람들끼리 쉽게 이해하고 이야기하기 위해 쓴다.
- 개발자, 기획자, 디자이너, 심지어 비개발자도 같은 그림을 보면서 시스템 구조나 흐름을 공유할 수 있다.
- 대표 종류로 `유즈케이스 다이어그램` , `클래스 다이어그램` , `시퀀스 다이어그램` 등이 있다.

[유즈케이스 다이어그램](./키워드%20정리.md#유즈케이스-다이어그램)

[클래스 다이어그램](./키워드%20정리.md#클래스-다이어그램)

[시퀀스 다이어그램](./키워드%20정리.md#시퀀스-다이어그램)

---

## 유즈케이스 다이어그램

- 사용자가 시스템을 어떻게 사용하는지를 그림으로 나타낸 것이다.
- 사용자(Actor)와 시스템 기능(Use Case)의 관계를 보여준다.
- 시스템 외부에서 누가 어떤 기능을 쓰는지 이해할 때 좋다.

예시

![유즈케이스 다이어그램](<./Images/유즈케이스 다이어그램.jpg>)


---

## 클래스 다이어그램

- 클래스, 속성, 메서드, 클래스 간 관계를 표현하는 UML 다이어그램이다.
- 설계 단계에서 구조를 보는 데 자주 사용한다.
- 상속, 연관, 집합 같은 관계도 표현 가능하다.

예시

![클래스 다이어그램](<./Images/클래스 다이어그램.jpg>)

---

## 시퀀스 다이어그램

- 객체들 사이에서 메시지가 시간 순서대로 어떻게 오가는지 표현한 UML 다이어그램이다.
- “누가 먼저 호출하고, 누가 응답하는지” 흐름을 보는 데 좋다.

예시

![시퀀스 다이어그램](<./Images/시퀀스 다이어그램.png>)

---

## .lib


#### 정적 라이브러리(.lib)

- 라이브러리 코드 자체가 통째로 들어있음.
- DLL처럼 별도로 메모리에 로드 하는게 아닌 이미 실행 파일 안에 포함됨.
- 컴파일할 때 내 프로그램 안에 붙어 들어감.
- 실행할 때 외부 파일이 덜 필요함.
- 오브젝트 파일 아카이브

##### 정적 라이브러리 실행 흐름
1. 소스 코드들을 컴파일해서 오브젝트 파일 생성
1. 오브젝트 파일들을 묶어서 `lib` 생성
1. 코드 내에서 함수를 호출함
1. 컴파일러가 함수를 보고 함수가 정의 밖에 있으면 링커가 `lib`을 훑음
1. `정적 lib`함수중 필요한 함수만 링커가 실행파일에 넣음


#### DLL용 import lib(.lib)

- 실제 코드가 아니라 DLL용 정보표
- "**이 함수는 어떤 DLL에 있다**"를 알려주는 링커용 안내서
- 컴파일할 때 필요, 실행할 때는 보통 진짜 DLL이 필요.
- DLL 파일내에 있음

##### DLL import lib 실행흐름
1. 링커가 실행 파일에 DLL연결 정보를 심어둠
1. 실행시 OS가 그걸 보고 DLL을 로드함

### 특징
- 배포가 단순함
- 실행 파일 크기가 커질 수 있음
- 수정 시 다시 빌드 필요

---

## DLL

- DLL은 Dynamic Link Library, 동적 라이브러리다.
- 프로그램 실행 중에 연결해서 사용하는 라이브러리다.
- 실행 파일과 분리되어 있다.
- 메모리에 로드 되기 전에는 사용자가 지정한 곳이나, 디스크 어딘가 파일에 존재한다.

#### 실행흐름
1. 프로그램이 실행됨.
1. 필요한 순간에 메모리에 DLL이 로드됨.
1. 프로그램이 DLL안의 함수 주소를 찾아 원하는 함수를 호출함.

### 특징
- 코드 재사용 가능
- 파일 교체로 업데이트 가능
- DLL이 없으면 실행 오류가 날 수 있음
- 실행 파일이 가벼워지고 필요할 때만 메모리에 올려서 효율적

예시

![DLL 실행흐름](<./Images/DLL 실행흐름avif.avif>)

---

## PC(Program Counter)

- 다음에 실행할 명령어의 주소를 저장하는 레지스터다.
- CPU는 PC(프로그램 카운터)를 보고 다음 명령어를 어디서 가져올지 결정한다.
- 명령어 포인터라고 부르는 CPU도 있음.

예시

![프로그램 카운터](<./Images/프로그램 카운터.jpg>)

프로그램 카운터에 1000이 저장돼있음 -> 메모리에서 가져올 명령어가 메모리 1000번지에 있다는 뜻

---

## IR(Instruction Register)

- 현재 실행 중인 명령어를 저장하는 레지스터다.
- 메모리에서 가져온 명령어를 일단 IR에 넣고 해석한다.
- 명령어 레지스터 라고도 부른다.
- 제어 장치는 IR(명령어 레지스터) 값을 읽고 해석한 뒤, 제어 신호를 보낸다.

예시

![명령어 레지스터](<./Images/명령어 레지스터(IR).jpg>)

##### 실행 흐름
1. 여러 과정을 통해 메모리 버퍼 레지스터(MBR)에 메모리의 값이 들어옴
1. 메모리 버터 레지스터에 있던 값이 명령어 레지스터로 이동함
1. 제어장치가 명령어 레지스터에 있는 값을 해석하고 제어신호를 발생

---

## AC(Accumulator)

- 연산 결과를 임시로 저장하는 레지스터다.
- 예전 구조의 CPU 설명에서 많이 나온다.
- ALU 연산 결과를 잠깐 담는 용도로 이해하면 된다.
- 누산기 라고도 한다.

[누산기](./키워드%20정리.md#cpu-연산장치---누산기)

---

## MAR(Memory Address Register)

- 접근할 메모리 주소를 저장하는 레지스터다.
- CPU가 메모리에 읽기/쓰기를 할 때 “어디를 볼지” 저장한다.

---

## MBR(Memory Buffer Register)

- 메모리와 주고받는 실제 데이터를 잠깐 저장하는 레지스터다.
- 메모리에서 읽어온 값이나 메모리에 쓸 값을 담는다.

---

## ALU

- ALU는 Arithmetic Logic Unit, 산술논리연산장치다.
- 덧셈, 뺄셈, 비교, 논리연산 같은 실제 계산을 담당한다.

---

## Decoder

- 디코더는 명령어를 해석해서 CPU 내부에 어떤 동작을 시킬지 결정하는 장치다.
- “이 명령은 덧셈이네”, “이 명령은 메모리 읽기네” 같은 해석을 담당한다.

---

## Shifter

- 비트를 왼쪽/오른쪽으로 이동시키는 장치다.
- 곱셈, 나눗셈, 비트 연산과 관련해서 자주 사용된다.

---

## Address Bus

- 주소를 전달하는 버스다.
- CPU가 메모리나 장치에 “몇 번지에 접근할지”를 보낼 때 사용한다.

[주소 버스](./키워드%20정리.md#cpu---주소-버스)

---

## Data Bus

- 실제 데이터를 전달하는 버스다.
- CPU와 메모리, 입출력 장치 사이에서 값이 오갈 때 사용한다.

[데이터 버스](./키워드%20정리.md#cpu---데이터-버스)

---

## Control Bus

- 제어 신호를 전달하는 버스다.
- 읽기, 쓰기, 인터럽트 같은 제어 정보를 주고받는다.

[제어 버스](./키워드%20정리.md#cpu---제어-버스)

---

## 명령어 사이클

- CPU가 명령어 하나를 처리하는 전체 과정이다.
- 보통 인출 → 해독 → 실행의 흐름으로 본다.
- 경우에 따라 간접 사이클, 인터럽트 사이클도 포함된다.

---

## 인출 사이클

- 메모리에서 명령어를 가져오는 단계다.
- PC가 가리키는 주소에서 명령어를 읽어온다.

---

## 간접 사이클

- 명령어가 실제 피연산자 주소를 바로 가지지 않고, 주소가 저장된 위치를 한 번 더 참조해야 할 때 수행되는 단계다.
- 한 번 더 메모리를 들러야 해서 추가 과정이 생긴다.

---

## 실행 사이클

- 해석이 끝난 명령어를 실제로 수행하는 단계다.
- 덧셈, 이동, 저장, 점프 같은 동작이 여기서 일어난다.

---

## 인터럽트 사이클

- CPU가 현재 하던 일을 잠깐 멈추고, 긴급한 요청을 처리하는 단계다.
- 처리 후에는 다시 원래 작업으로 복귀할 수 있다.

---

## 캐시 메모리

- CPU와 메인 메모리 사이에 있는 빠른 임시 저장소다.
- 자주 쓰는 데이터를 가까운 곳에 둬서 속도를 높인다.
- RAM보다 작지만 훨씬 빠르다.

---

## 캐시 메모리 지역성

- 프로그램은 특정 데이터나 명령어를 몰아서 쓰는 경향이 있다.
- 이 성질을 지역성(Locality)이라고 한다.

### 종류
- 시간 지역성: 최근 쓴 것을 다시 쓰는 경향
- 공간 지역성: 근처 주소를 같이 쓰는 경향

---

## 캐시메모리 일관성

- 여러 캐시가 같은 메모리 값을 들고 있을 때 값이 서로 다르면 문제가 생긴다.
- 이 값을 서로 맞추는 문제를 캐시 일관성이라고 한다.
- 멀티코어 환경에서 특히 중요하다.

---

## 캐시메모리 매핑

- 메인 메모리의 데이터를 캐시에 어떤 방식으로 배치할지 정하는 방법이다.

### 종류
- 직접 매핑
- 연관 매핑
- 집합 연관 매핑

---

## 가상메모리

- 실제 RAM보다 더 큰 메모리를 쓰는 것처럼 보이게 하는 기술이다.
- 보조기억장치 일부를 메모리처럼 사용한다.
- 프로그램마다 자기만의 큰 메모리 공간을 가진 것처럼 보이게 해준다.

---

## 가상메모리 관리정책

- 어떤 페이지를 메모리에 올릴지
- 어떤 페이지를 내릴지
- 언제 교체할지  
이런 규칙을 관리하는 방식이다.

### 예시
- 페이지 교체 정책
- 할당 정책
- 적재 정책

---

## 가상메모리 페이징

- 메모리를 같은 크기의 페이지 단위로 나눠 관리하는 방식이다.
- 외부 단편화를 줄이기 좋다.
- 주소 변환을 위해 페이지 테이블이 필요하다.

---

## 가상메모리 세그먼테이션

- 프로그램을 의미 단위로 나눠서 관리하는 방식이다.
- 코드, 데이터, 스택처럼 논리적 단위로 구분한다.
- 세그먼트마다 크기가 다를 수 있다.

---

## 가상메모리 페이지드 세그멘테이션

- 세그먼테이션과 페이징을 합친 방식이다.
- 논리적으로는 세그먼트로 나누고, 실제 관리에서는 페이지 단위로 처리한다.

---

## Thrashing

- 스래싱은 페이지 교체가 너무 자주 일어나서 CPU가 일보다 메모리 교체에 더 많은 시간을 쓰는 상태다.
- 프로그램은 거의 일을 못 하고 계속 페이지 넣고 빼기만 한다.
- 성능이 급격히 떨어진다.

---

## 메모리 인터리빙

- 메모리를 여러 뱅크로 나눠서 번갈아 접근하게 하여 속도를 높이는 방식이다.
- 동시에 더 효율적으로 데이터를 공급할 수 있다.

---

## 메모리 단편화

- 메모리를 사용하고 해제하는 과정에서 공간이 쪼개지는 현상이다.
- 필요한 메모리가 충분히 남아 있어도, 조각나 있으면 활용이 어렵다.

---

## 메모리 단편화 - 내부

- 할당한 메모리 블록 안에서 남는 공간이 생기는 현상이다.
- 예를 들어 10칸이 필요한데 16칸을 통째로 할당받으면 안쪽에 남는 공간이 생긴다.

---

## 메모리 단편화 - 외부

- 빈 메모리는 충분한데 여기저기 흩어져 있어서 큰 덩어리로 못 쓰는 현상이다.
- 메모리 사이사이가 조각난 상태라고 보면 된다.

---

## 메모리 단편화 - 압축

- 흩어진 메모리 조각을 한쪽으로 몰아서 큰 빈 공간을 만드는 방식이다.
- 외부 단편화 해결 방법 중 하나다.
- 다만 이동 비용이 든다.

---

## 메모리 단편화 - 메모리 풀

- 자주 쓰는 크기의 메모리를 미리 여러 개 만들어두고 재사용하는 방식이다.
- 할당/해제가 반복되는 상황에서 단편화를 줄이고 속도를 높이는 데 도움 된다.
- 게임 개발에서도 많이 쓴다.

---

## 가비지 컬렉션

- 더 이상 사용하지 않는 메모리를 자동으로 정리하는 기능이다.
- C#, Java 같은 언어에서 대표적이다.
- 개발자가 직접 `free`하지 않아도 되지만, 언제 정리될지 완전히 마음대로 조절하긴 어렵다.

---

## 컴파일러

- 전체 소스코드를 한 번에 번역해서 기계어나 실행 파일로 바꾸는 프로그램이다.
- 실행 전에 번역을 끝내는 방식이다.

### 특징
- 실행 속도 빠름
- 번역 단계 필요
- 오류를 미리 많이 발견 가능

---

## 인터프리터

- 코드를 한 줄씩 읽고 바로 실행하는 방식이다.
- 실행하면서 해석한다.

### 특징
- 실행 전 전체 번역이 필요 없음
- 디버깅이 편한 경우가 있음
- 일반적으로 컴파일 방식보다 느릴 수 있음

---

## 컴파일러 vs 인터프리터

- 컴파일러는 “미리 다 번역하고 실행”
- 인터프리터는 “읽으면서 바로 실행”

### 비유
- 컴파일러 = 시험 전에 번역본을 다 만들어두는 것
- 인터프리터 = 읽으면서 바로바로 통역하는 것

---

## GPU

- GPU는 Graphics Processing Unit, 그래픽 처리 장치다.
- 원래 목적은 화면에 보이는 3D/2D 그래픽을 빠르게 계산해서 출력하는 것이었다.
- 하지만 구조상 “같은 계산을 아주 많이 반복하는 일”에 강해서, 지금은 그래픽뿐 아니라 연산용으로도 많이 쓰인다.

### 왜 GPU가 따로 필요할까?

- 게임에서 화면을 그릴 때는 한두 개 계산이 아니라, 엄청 많은 픽셀과 정점(Vertex), 텍스처 계산이 동시에 일어난다.
- 이런 작업을 CPU 혼자 하면 너무 무겁다.
- 그래서 화면 계산 전용으로 훨씬 병렬 처리에 강한 GPU가 쓰인다.



### 게임에서 GPU가 많이 하는 일

- 캐릭터, 배경, 이펙트 렌더링
- 조명 계산
- 그림자 처리
- 후처리 효과
- 화면 해상도에 따른 픽셀 계산

---

## GPGPU

- GPGPU는 General Purpose computing on GPU의 줄임말이다.
- 뜻은 “GPU를 그래픽 말고 일반 계산에도 사용한다”는 의미다.
- 원래 GPU는 그림을 그리는 용도였지만, 계산 능력이 너무 강해서 과학 계산, AI, 시뮬레이션에도 쓰이게 되었다.

### 왜 쓰는가?

- GPU는 같은 계산을 수천 개 단위로 동시에 처리하는 데 강하다.
- 그래서 행렬 계산, 물리 시뮬레이션, 이미지 처리, 딥러닝처럼 반복 계산이 많은 작업에서 엄청 빠르다.

### 예시

- 딥러닝 학습
- 영상 필터 처리
- 대규모 물리 시뮬레이션
- 입자(Particle) 계산

---

## CPU vs GPU

- CPU와 GPU는 둘 다 연산 장치지만, 잘하는 일이 다르다.
- CPU는 다양한 종류의 일을 유연하게 처리하는 데 강하고, GPU는 똑같은 계산을 대량으로 동시에 처리하는 데 강하다.

| 항목 | CPU | GPU |
|------|-----|-----|
| 주 역할 | 범용 연산, 제어 | 대량 병렬 연산 |
| 코어 수 | 적음 | 많음 |
| 강점 | 복잡한 판단, 분기, 제어 | 반복 계산, 병렬 처리 |
| 예시 | 게임 로직, 입력 처리, 운영체제 작업 | 렌더링, 셰이더, 대규모 계산 |

### 게임 기준으로 보면

- CPU는 플레이어 입력, AI 판단, 게임 규칙, 씬 관리 같은 걸 많이 담당한다.
- GPU는 “결국 화면에 무엇을 어떻게 보여줄지”를 빠르게 계산한다.



---

## CUDA

- CUDA는 NVIDIA가 만든 GPU 프로그래밍 플랫폼이다.
- 개발자가 GPU를 그래픽 출력뿐 아니라 일반 연산에도 활용할 수 있게 해준다.
- 즉, GPU에게 “그림만 그리지 말고 계산도 해”라고 시키기 쉽게 만든 도구라고 보면 된다.

### 특징

- NVIDIA 그래픽카드 기반
- 병렬 연산에 특화
- AI, 과학 계산, 시뮬레이션 분야에서 많이 사용

### 주의

- CUDA는 보통 NVIDIA 환경과 강하게 연결된다.
- 그래서 “GPU 연산” 전체를 CUDA라고 생각하면 안 되고, CUDA는 그중 한 플랫폼이다.

---

## RAM vs VRAM

- RAM은 일반 시스템 메모리다.
- VRAM은 비디오 메모리, 즉 GPU가 그래픽 처리를 위해 주로 사용하는 메모리다.

### RAM

- CPU가 주로 사용한다.
- 프로그램 실행, 데이터 저장, 운영체제 동작에 쓰인다.

### VRAM

- GPU가 주로 사용한다.
- 텍스처, 프레임 버퍼, 셰이더 데이터, 모델 데이터 등을 저장한다.

### 차이 느낌

- RAM = 게임 전체 운영에 필요한 작업 공간
- VRAM = 화면을 실제로 그리는 데 필요한 전용 작업 공간

### 게임에서 중요한 이유

- 고해상도 텍스처, 큰 모델, 많은 이펙트를 쓰면 VRAM 사용량이 커진다.
- VRAM이 부족하면 끊김, 로딩 지연, 프레임 저하가 생길 수 있다.

---

## Compute Shader

- Compute Shader는 GPU로 일반 계산을 수행하기 위한 셰이더다.
- 원래 셰이더는 화면을 그리는 과정에서 쓰였는데, Compute Shader는 “화면용”이 아니라 “계산용”에 더 가깝다.

### 어디에 쓰는가?

- 물리 계산
- 군중/입자 처리
- 이미지 후처리
- 대량 데이터 병렬 계산

### 왜 좋은가?

- 같은 계산을 많은 데이터에 동시에 적용할 수 있다.
- CPU보다 훨씬 빠르게 끝나는 경우가 많다.

### 게임 개발에서 느낌

- 몬스터 떼, 비, 눈, 입자 같은 걸 CPU가 다 계산하면 무거운데, 일부를 GPU 계산으로 넘기면 효율이 좋아질 수 있다.

---

## 프로세스

- 프로세스(Process)는 실행 중인 프로그램이다.
- 단순히 저장된 `.exe` 파일이나 앱 파일이 아니라, 메모리에 올라와 실제로 돌아가고 있는 상태를 말한다.

### 예시

- 크롬을 실행하면 크롬 프로세스가 생긴다.
- 게임을 실행하면 게임 프로세스가 생긴다.

### 중요한 점

- 프로그램 = 디스크에 저장된 파일
- 프로세스 = 메모리에 올라와 실행 중인 프로그램

### 프로세스가 가지는 것

- 코드
- 데이터
- 메모리 공간
- 실행 상태
- 운영체제가 관리하기 위한 정보

---

## 프로세스 메모리 영역

- 프로세스는 메모리 안에서 한 덩어리로만 있는 게 아니라, 역할별로 나눠서 관리된다.
- 대표적으로 코드 영역, 데이터 영역, 힙 영역, 스택 영역으로 구분한다.

### 왜 나누는가?

- 각 데이터의 성격이 다르기 때문이다.
- 프로그램 코드, 전역 변수, 동적 생성 데이터, 함수 호출 데이터는 관리 방식이 다르다.

### 쉽게 비유하면

- 코드 영역 = 설명서 보관함
- 데이터 영역 = 오래 유지되는 공용 창고
- 힙 영역 = 필요할 때 빌려 쓰는 창고
- 스택 영역 = 함수 호출할 때 잠깐 쓰는 작업대

---

## 코드 영역

- 코드 영역(Text Segment)은 실행할 명령어가 저장되는 공간이다.
- 우리가 작성한 프로그램의 기계어 명령들이 올라가는 곳이다.
- 보통 실행 중 자주 바뀌지 않으며 읽기 전용 성격이 강하다.

### 특징

- 프로그램 동작 명령 저장
- 보통 수정되지 않음
- 여러 실행 흐름이 같은 코드를 공유 가능

---

## 데이터 영역

- 데이터 영역은 프로그램이 시작할 때부터 끝날 때까지 유지되는 데이터가 저장되는 영역이다.
- 주로 전역 변수(static 포함)가 들어간다.

### 예시

- 전역 변수
- static 변수

### 특징

- 프로그램 시작 시 할당
- 프로그램 종료 시 해제
- 함수가 끝나도 사라지지 않음

---

## 힙 영역

- 힙 영역은 실행 중에 동적으로 메모리를 할당받는 공간이다.
- 필요할 때 만들고, 더 이상 필요 없으면 해제한다.

### 예시

- C에서 `malloc`
- C++에서 `new`
- C#에서 `new`로 만든 참조형 객체들

### 특징

- 런타임 중 크기 결정 가능
- 유연하지만 관리 비용이 있음
- 잘못 쓰면 메모리 누수나 단편화 문제가 생길 수 있음

### 쉽게 말하면

- “필요할 때 창고를 빌려 쓰는 공간”

---

## 스택 영역

- 스택 영역은 함수 호출 시 필요한 정보가 쌓이는 공간이다.
- 지역 변수, 매개변수, 반환 주소 등이 저장된다.

### 특징

- 함수 호출 시 쌓임
- 함수 종료 시 자동 제거
- 매우 빠름
- 구조가 단순함

### 왜 빠를까?

- 스택은 보통 맨 위에 쌓고 맨 위에서 꺼내는 LIFO 구조라서 관리가 단순하다.
- 운영체제가 복잡하게 빈 공간을 찾을 필요가 적다.



---

## 파일 포맷

- 파일 포맷은 데이터를 어떤 구조와 규칙으로 저장할지를 정한 형식이다.
- 확장자가 같아 보여도 내부 저장 방식과 목적이 다를 수 있다.

### 왜 중요할까?

- 같은 이미지라도 JPG, PNG, BMP는 압축 방식과 용도가 다르다.
- 같은 소리나 영상도 포맷에 따라 용량, 품질, 호환성이 다르다.

---

## Jpg

- JPG는 사진 저장에 많이 쓰이는 이미지 포맷이다.
- 손실 압축 방식을 사용해서 용량을 크게 줄일 수 있다.
- 대신 압축 과정에서 일부 화질 손실이 생길 수 있다.

### 장점
- 용량이 작아지기 쉽다.
- 사진 저장에 적합하다.

### 단점
- 저장할수록 품질 손실이 누적될 수 있다.
- 투명도 지원이 약하다.

---

## Bmp

- BMP는 비트맵 이미지 포맷이다.
- 구조가 단순하고 압축이 적어서 용량이 큰 편이다.

### 특징
- 품질 손실이 거의 없다.
- 파일 크기가 크다.
- 예전 윈도우 환경에서 많이 보였다.

---

## Tga

- TGA는 게임 그래픽 작업에서 예전부터 자주 쓰이던 이미지 포맷이다.
- 알파 채널(투명도) 처리와 텍스처 작업에서 편하게 쓰이는 경우가 많다.

### 특징
- 게임 리소스 작업에서 자주 등장
- 투명 정보 처리 가능
- 용량은 PNG보다 큰 편일 수 있음

---

## PNG

- PNG는 무손실 압축 이미지 포맷이다.
- 화질 손실 없이 저장할 수 있고, 투명도 지원이 좋다.

### 장점
- 아이콘, UI, 스프라이트에 적합
- 투명 배경 사용 가능
- 품질 유지에 유리

### 단점
- 사진 저장 시 JPG보다 용량이 커질 수 있음

---

## Wav

- WAV는 대표적인 오디오 파일 포맷이다.
- 비압축 또는 저압축 형태가 많아 음질이 좋다.
- 대신 파일 용량이 큰 편이다.

### 게임에서 자주 쓰는 이유

- 효과음은 짧고 즉시 재생이 중요해서 WAV가 편한 경우가 있다.
- 다만 프로젝트에 따라 mp3, ogg도 함께 쓴다.

---

## Avi

- AVI는 영상과 음성을 담을 수 있는 비디오 포맷이다.
- 오래된 형식이지만 기본 개념 설명에서 자주 등장한다.

### 특징
- 컨테이너 형식으로 이해하는 경우가 많다.
- 압축 방식에 따라 품질과 용량이 달라질 수 있다.
- 요즘은 mp4 같은 형식이 더 자주 보인다.

---

## Thread

- 스레드(Thread)는 프로세스 안에서 실제 실행 흐름을 담당하는 단위다.
- 한 프로세스 안에는 하나 이상의 스레드가 있을 수 있다.

### 예시

- 게임 메인 루프 실행
- 백그라운드 로딩
- 네트워크 처리

### 핵심

- 프로세스는 “큰 작업 단위”
- 스레드는 “그 안에서 실제로 움직이는 일꾼”

---

## Process

- Process도 프로세스를 뜻한다.
- 저장된 프로그램이 실제 실행 상태가 되면 프로세스가 된다.
- 운영체제는 프로세스를 단위로 메모리와 자원을 관리한다.

---

## Process vs Thread

- 프로세스는 독립된 실행 단위이고, 스레드는 프로세스 내부의 실행 흐름이다.
- 프로세스는 메모리 공간이 분리되어 있고, 스레드는 같은 프로세스 안에서 자원을 공유한다.

| 항목 | 프로세스 | 스레드 |
|------|----------|--------|
| 의미 | 실행 중인 프로그램 | 프로세스 내부 실행 흐름 |
| 메모리 | 독립적 | 같은 프로세스 내 공유 |
| 생성 비용 | 큼 | 상대적으로 작음 |
| 통신 | 복잡함 | 공유 자원 사용 가능 |
| 위험 | 독립적이라 영향 분리 쉬움 | 잘못 건드리면 동기화 문제 발생 |

### 주의

- 스레드는 메모리를 공유하니까 빠르고 편할 수 있다.
- 대신 동시에 같은 데이터에 접근하면 충돌 문제가 생길 수 있다.

---

## PCB(Process Control Block)

- PCB는 Process Control Block, 프로세스 제어 블록이다.
- 운영체제가 각 프로세스를 관리하기 위해 저장해두는 정보 묶음이다.
- 프로세스의 “관리 카드” 같은 느낌으로 이해하면 좋다.

### 들어가는 정보 예시
- Process ID
- 프로세스 상태
- 프로그램 카운터
- CPU 레지스터 정보
- 스케줄링 정보
- 메모리 관련 정보

---

## TCB(Thread Control Block)

- TCB는 Thread Control Block, 스레드 제어 블록이다.
- 운영체제가 각 스레드를 관리하기 위한 정보 묶음이다.
- PCB가 프로세스 전체 관리 카드라면, TCB는 스레드 개별 관리 카드다.

### 들어가는 정보 예시
- Thread ID
- 스레드 상태
- PC
- 레지스터 정보
- 스케줄링 관련 정보

---

## PCB - Process id

- Process ID는 각 프로세스를 식별하기 위한 고유 번호다.
- 운영체제가 여러 프로세스를 구분할 때 사용한다.
- 사람 이름이 아니라 주민등록번호처럼 “겹치지 않는 식별자”라고 생각하면 쉽다.

---

## TCB - Thread Id

- Thread ID는 각 스레드를 식별하기 위한 고유 번호다.
- 한 프로세스 안에 여러 스레드가 있어도 각각을 따로 구분할 수 있다.

---

## PCB - Process Status

- 프로세스 상태 정보다.
- 현재 이 프로세스가 어떤 상태에 있는지 운영체제가 기록한다.

### 대표 상태
- New: 생성됨
- Ready: 실행 준비 완료
- Running: 실행 중
- Waiting: 대기 중
- Terminated: 종료됨

---

## TCB - Thread Status

- 스레드 상태 정보다.
- 현재 스레드가 실행 중인지, 대기 중인지, 준비 상태인지 등을 기록한다.
- 프로세스와 비슷하지만, 더 작은 실행 단위를 관리하는 느낌이다.

---

## CPU register

- CPU 레지스터는 CPU 내부에 있는 아주 빠른 저장 공간이다.
- 연산 중인 값, 주소, 상태 정보를 잠깐 저장한다.
- RAM보다 훨씬 빠르며, CPU가 즉시 접근해야 하는 값들을 담는다.

### 왜 중요한가?

- CPU는 계산할 때 계속 메모리만 왔다 갔다 하면 느리다.
- 그래서 지금 당장 필요한 값을 레지스터에 넣고 빠르게 처리한다.


---

## CPU Register - PC(Program Counter)

- PC는 다음에 실행할 명령어의 주소를 저장하는 레지스터다.
- CPU는 PC를 보고 “다음에 어디 코드로 갈지” 결정한다.

### 중요 포인트
- 순차 실행이면 다음 주소로 증가
- 점프/분기 명령이 있으면 다른 주소로 바뀜
- 프로그램 흐름 제어의 핵심

---

## PCB - PC

- 프로세스가 실행을 멈췄다가 다시 이어서 실행될 수 있으려면, 어디까지 했는지 기억해야 한다.
- 그때 프로세스의 PC 값이 PCB에 저장된다.
- 즉, 프로세스 문맥 전환(Context Switch) 시 복귀 위치를 기록해두는 것이다.

---

## TCB - PC

- 스레드도 마찬가지로, 중간에 멈췄다가 다시 이어서 실행할 수 있어야 한다.
- 그래서 스레드의 PC 값은 TCB에 저장된다.
- 멀티스레드 환경에서 특히 중요하다.

---

## CPU Register - IR

- IR은 현재 실행 중인 명령어를 저장하는 레지스터다.
- 메모리에서 명령어를 가져오면 CPU는 그걸 IR에 넣고 해석한다.

### 흐름
- 메모리에서 명령어 읽기
- IR에 저장
- 제어장치가 해석
- 필요한 동작 수행

---

## CPU Register - MAR

- MAR은 Memory Address Register다.
- CPU가 접근하려는 메모리 주소를 저장한다.
- 쉽게 말해 “어디 메모리를 볼지”를 적어두는 칸이다.

---

## CPU Register - MBR

- MBR은 Memory Buffer Register다.
- 메모리와 주고받는 실제 데이터를 잠깐 저장하는 레지스터다.
- 읽어온 값이나 저장할 값을 담는다.

### MAR와 MBR 차이
- MAR = 주소
- MBR = 데이터

---

## CPU Register - GPR

- GPR은 General Purpose Register, 범용 레지스터다.
- 특정 한 가지 용도만 하는 게 아니라, 여러 일반 연산에 폭넓게 사용된다.
- 계산 중간값 저장, 임시 값 저장 등에 많이 사용한다.

---

## CPU Register - AC

- AC는 Accumulator, 누산기다.
- 연산 결과를 임시로 저장하는 데 사용되는 레지스터다.
- 예전 CPU 구조 설명에서 자주 등장하며, 기본 연산 구조를 이해할 때 중요하다.

### 느낌
- 계산기 화면에 지금 결과가 잠깐 떠 있는 칸 같은 느낌

---

## CPU Register(Flag Register)

- Flag Register는 연산 결과의 상태를 저장하는 레지스터다.
- 계산 결과가 0인지, 음수인지, 자리올림이 있었는지 같은 정보를 기록한다.

### 왜 필요할까?

- 조건 분기에서 사용된다.
- 예를 들어 “결과가 0이면 점프” 같은 판단을 할 때 이 플래그 값을 본다.

### 예시 상태
- Zero Flag
- Sign Flag
- Carry Flag
- Overflow Flag

---

## CPU Register - SP(Stack Pointer)

- SP는 Stack Pointer다.
- 현재 스택의 맨 위 위치를 가리키는 레지스터다.
- 함수 호출, 반환, 지역 변수 관리에 매우 중요하다.

### 왜 중요할까?

- 함수가 호출되면 스택에 값이 쌓인다.
- 함수가 끝나면 스택에서 값이 빠진다.
- 이때 SP가 “지금 스택의 어디까지 사용 중인지” 알려준다.

---

## CPU - 제어장치

- 제어장치는 CPU 내부에서 전체 동작 순서를 통제하는 장치다.
- 메모리에서 명령어를 가져오고, 그 명령어를 해석하고, 각 장치에 어떤 동작을 할지 제어 신호를 보낸다.

### 무슨 역할을 하나?

- 명령어 해석
- 실행 순서 제어
- ALU, 레지스터, 메모리 사이 동작 조정
- 읽기/쓰기 같은 제어 신호 전달


### CPU 안 역할 분담 느낌
- 제어장치 = 지시
- ALU = 계산
- 레지스터 = 초고속 임시 저장소

### 흐름 예시
1. PC가 다음 명령어 위치를 가리킨다.
2. 명령어를 메모리에서 가져온다.
3. IR에 저장한다.
4. 제어장치가 해석한다.
5. ALU나 메모리, 레지스터에 필요한 동작을 지시한다.
6. 결과를 저장하고 다음 명령으로 넘어간다.

---
## CPU - 연산장치

CPU의 연산장치(ALU, Arithmetic Logic Unit)는 산술 연산과 논리 연산을 실제로 수행하는 부분이다.  
덧셈, 뺄셈, 곱셈, 나눗셈 같은 산술 연산과 AND, OR, NOT 같은 논리 연산을 처리하며, CPU가 명령어를 실행할 때 핵심 역할을 맡는다.  
연산장치는 단순히 계산만 하는 것이 아니라, 계산에 필요한 데이터를 받아들이고, 결과를 만들어내고, 다음 단계에서 사용할 수 있도록 넘겨주는 역할까지 포함한다.

---

## CPU 연산장치 - 가산기

가산기는 2진수 덧셈을 수행하는 **연산 회로**이다.  
컴퓨터 내부의 숫자는 대부분 2진수로 표현되므로, 가산기는 CPU가 계산을 수행할 때 가장 기본이 되는 장치라고 볼 수 있다.  
뺄셈이나 다른 연산도 내부적으로는 덧셈 형태로 바꿔 처리하는 경우가 많기 때문에, 가산기는 연산장치의 중심적인 역할을 한다.

### 예시
`3 + 5`를 계산할 때 사람은 십진수로 바로 더하지만, 컴퓨터는 **이를 2진수로 변환한 뒤 덧셈을 수행**한다.  
이때 실제로 2진수 덧셈을 담당하는 회로가 가산기이다.

---
## CPU 연산장치 - 누산기

누산기(Accumulator)는 연산 결과를 임시로 저장하는 **레지스터**이다.  
계산을 한 번 하고 끝나는 것이 아니라, 그 결과를 다음 계산의 입력으로 다시 사용해야 할 때 누산기가 중요해진다.  
연산 결과를 계속 누적해서 저장하는 성격이 있기 때문에 누산기라는 이름이 붙었다.

### 예시
`10 + 5`의 결과가 15라면, 이 15를 누산기에 저장해두고 다음 계산에서 다시 사용할 수 있다.  
즉, 누산기는 계산 중간값을 보관하는 작업 공간 역할을 한다.

---
## CPU 연산장치 - 보수기

보수기는 뺄셈을 수행하기 위해 입력된 값을 보수로 변환하는 **연산 회로**이다.  
컴퓨터는 뺄셈을 직접 처리하기보다, 보수를 이용해 덧셈 형태로 바꾸어 계산하는 방식을 자주 사용한다.  
이 방식은 하드웨어 구성이 단순해지고 계산 처리도 효율적이기 때문에 널리 쓰인다.

### 예시
`A - B`를 계산할 때, 컴퓨터는 `A + (B의 보수)` 형태로 바꿔 처리할 수 있다.  
이때 B를 보수 형태로 바꾸는 역할을 하는 것이 보수기이다.

---
## CPU 시스템 버스

시스템 버스(System Bus)는 CPU와 메모리, 그리고 입출력 장치들 사이를 연결해주는 공통 전송 경로이다.
컴퓨터 내부의 여러 장치가 서로 데이터를 주고받고 명령을 전달할 수 있는 통로라고 이해하면 된다.  
시스템 버스가 없으면 CPU는 메모리나 주변 장치와 효율적으로 통신할 수 없다.

### 주소 버스
주소 버스는 **데이터를 주고받을 위치를 지정하는 통로**이다.  
CPU가 메모리의 몇 번지에 접근할지 알려줄 때 사용된다.

### 데이터 버스
데이터 버스는 실제 데이터가 이동하는 통로이다.  
**메모리에서 CPU로** 값이 들어오거나, **CPU에서 메모리나 I/O 장치로** 결과가 전달될 때 사용된다.

### 제어 버스
제어 버스는 읽기, 쓰기, 시작, 종료 같은 제어 신호를 전달하는 통로이다.  
CPU가 다른 장치에게 어떤 동작을 해야 하는지 지시할 때 사용된다.

예시

![시스템 버스](<./Images/시스템 버스.jpg>)

[주소 버스](./키워드%20정리.md#cpu---주소-버스)  
[데이터 버스](./키워드%20정리.md#cpu---데이터-버스)  
[제어 버스](./키워드%20정리.md#cpu---제어-버스)

---

## CPU - 주소 버스

주소 버스는 **시스템 버스**의 구성 요소중 하나이다. 메모리나 입출력 장치에게 주소를 전달하는 통로이다.  
주소 버스에는 데이터 자체가 아니라 데이터가 있는 번지(위치 정보)가 흐른다.  

### 특징
- 주소 버스는 일반적으로 CPU => 메모리/I/O 장치 방향의 단방향 버스이다.
- 주소 버스의 폭은 접근 가능한 주소 공간의 크기를 의미한다.

### 흐름 예시
1. CPU의 MAR(메모리 주소 레지스터)에 100번지 주소를 넣음
1. *MAR의 주소값이 **주소 버스**를 통해 메모리에 전달됨*
1. 제어 버스로 CPU가 읽기 신호를 보냄
1. 메모리는 100번지에 있떤 실제 값을 데이터 버스로 CPU에 보냄

### 주의
- 실제 데이터는 주소 버스가 아니라 데이터 버스로 이동한다.

---

## CPU - 데이터 버스

데이터 버스는 **시스템 버스**의 구성 요소중 하나이다.  
CPU, 메모리, 입출력 장치 사이에서 실제 데이터 자체를 옮기는 통로이다.

### 특징
- 데이터 버스는 데이터 `읽기`와 `쓰기`를 전부 하기 때문에 양방향 버스이다.
- 컴퓨터 입장에서 0과 1로 표현 가능한건 전부 데이터 버스를 타고 이동 가능하다.
- 데이터 버스의 폭은 한 번에 전달 가능한 데이터량을 의미한다.

### `읽기`신호/`쓰기`신호
- `읽기`신호는 메모리에 있던 데이터 값을 CPU가 읽어 들이려 요청하는 신호이다.
- `쓰기`신호는 CPU가 데이터 값을 메모리에 저장하려 보내는 신호이다.

### 흐름 예시(`읽기`)
1. CPU가 MAR(메모리 주소 레지스터)에 `100번지` 주소를 넣음
1. 주소 버스를 통해 `100번지`를 메모리에 전달함
1. 제어 버스로 `읽기` 신호를 보냄
1. *메모리가 `100번지`의 실제 값을 **데이터 버스**로 CPU에 보냄*
1. CPU는 받은 값을 MBR(메모리 버퍼 레지스터)/MDR(메모리 데이터 레지스터) 같은 데이터 레지스터에 잠깐 저장하고 사용함

### 흐름 예시(`쓰기`)
1. CPU가 MAR(메모리 주소 레지스터)에 주소 `200번지`를 넣음
1. 주소 버스로 `200번지`를 메모리에 전달함
1. CPU가 저장할 값을 MBR(메모리 버퍼 레지스터)/MDR(메모리 데이터 레지스터)에 준비함
1. 제어 버스로 `쓰기` 신호를 보냄
1. ***데이터 버스**를 통해 데이터 값이 메모리 `200번지`로 이동함*

---

## CPU - 제어 버스

제어 버스는 **시스템 버스**의 구성 요소중 하나이다.  
CPU, 메모리, 입출력 장치가 서로 작업할 때 제어 신호를 보내는 통로이다.  
작업 과정의 **종류/순서/타이밍**을 조정한다.

### 특징
- 크게 `명령/제어 신호`와 `상태/응답 신호`를 보낸다.
- CPU가 보내는 신호도 있고 장치가 CPU에데 보내는 신호도 있기 때문에 방향이 신호마다 다르다.

### 제어 신호
장치에게 **특정 동작을 지시**하거나 **장치의 상태**를 알리는 전기 신호이다.

### 대표 제어 신호
- `읽기`신호
    - 메모리에 있던 데이터 값을 CPU가 읽어 들이려 요청하는 신호이다.
    - [읽기 신호](./키워드%20정리.md#읽기신호쓰기신호)

- `쓰기` 신호
    - CPU가 데이터 값을 메모리에 저장하려 보내는 신호이다.
    - [쓰기 신호](./키워드%20정리.md#읽기신호쓰기신호)

- `Interrupt Request(인터럽트 요구)` 신호
    - 입출력 장치가 CPU에게 인터럽트를 알리는 신호이다.

- `Interrupt Acknowledge(인터럽트 인정)` 신호
    - CPU가 인터럽트 요청을 받고 처리를 응답하는 신호이다.

- `Ready / Busy / 전송 확인` 신호
    - Ready : **데이터 전송 준비가 끝남**을 알리는 신호이다.
    - Busy : 현재 **다른 작업중이라 바로 처리를 못함**을 알리는 신호이다.
    - 전송 확인 : **데이터 전송이 완료됨**을 알리는 신호이다.

---
## C# - 값 타입
값 타입은 변수가 실제 **값 자체를 갖는 타입**이다. 값 타입은 **실제 데이터를 복사**한다.

### 대표적인 값 타입
`int` / `float` / `double` / `bool` / `char` / `struct` / `enum` / `Vector2` / `Vector3` / `Color` / `Quaternion`
- Vector2, Vector3, Color도 구조체(struct)라서 값 타입이다.

### 값 타입 예시
```csharp
int a = 10;
int b = a;

b = 20;

Debug.Log(a); // 10
Debug.Log(b); // 20
```
- `b=a;`를 하면 a의 값 10이 b에 복사가 된다.(**값 자체/실제 데이터 복사**)
- 그 뒤에 b만 따로 20으로 바꿔도 a와 b는 별개 이므로 a는 그대로 10으로 남고 b만 20이 된다.
- 값 타입은 각각 **자기 값을 따로 가지고 있다**.

| 타입    | =로 복사되는 것    | 복사 뒤 관계   |
| ----- | ------------ | --------- |
| 값 타입  | 실제 데이터       | 서로 독립     |
| 참조 타입 | 객체를 가리키는 참조값 | 같은 객체를 공유 |

---
## C# - 참조 타입
참조 타입은 변수에 실제 객체 데이터가 아니라, 그 **객체를 가리키는 참조(reference)가 들어가는 타입**이다.

### 대표적인 참조 타입
`class` / `string` / `array` / `List< T>` / `Dictionary` / `delegate` / `interface` / `object`

### 참조타입 예시
```csharp
public class Player
{
    public int hp;
}
```  
```csharp
Player playerA = new Player();
playerA.hp = 100;

Player playerB = playerA;
playerB.hp = 50;

Debug.Log(playerA.hp); // 50
Debug.Log(playerB.hp); // 50
```
- `playerB = playerA`는 Player 클래스 객체를 **새로 복사한게 아니다.**
- 둘다 같은 Player **객체 한 개를 가리키는 것**이다.
- `playerB.hp = 50`은 같은 `playerA.hp`가 가리키는 객체와 같은 객체의 hp를 바꾸는 것이므로,  
   `playerA.hp`로 봐도 똑같은 50이 나온다.

| 타입    | 복사되는 것    | 복사 뒤 관계   |
| ----- | ------------ | --------- |
| 값 타입  | 실제 데이터       | 서로 독립     |
| 참조 타입 | 객체를 가리키는 참조값 | 같은 객체를 공유 |

---

## C# - 박싱(Boxing)
박싱은 **값 타입을 객체로 감싸는 것**이다.   
[값 타입](./키워드%20정리.md#c---값-타입)

### object
`object`는 C#에서 **거의 모든 타입을 담을 수 있는 가장 기본적인 타입**이다.  
값 타입도 `object`변수에 넣을 수 있는데, **그냥 들어가는게 아닌 박싱이 일어난다**

### 박싱 예시
```csharp
int score = 100;

object data = score; //여기서 박싱이 일어난다.
```
- 박싱 후 `object`타입인 `data`안에 `100`이 들어간 상태이다.
- `int`같은 값 타입을 `object`에 넣을 때, **값을 새로운 `object`인스턴스 안에 감싸고 값을 복사** 한다.
- 이 과정은 **자동으로 일어나서 캐스팅(object)을 안써도 된다**.
```csharp
int score = 100;
object data = score;

score = 200;

Debug.Log(score); // 200
Debug.Log(data);  // 100
```
- `data = score`할 때 박싱이 되어 들어갔기 때문에, 나중에 `score`을 `200`으로 바꿔도 `data`안의 값은 바뀌지 않는다.
```csharp
int score = 100;
object data = score;

data = 200;

Debug.Log(score); // 100
Debug.Log(data);  // 200
```
- 반대로 이렇게 하면 원본이 바뀌지 않는다.
- 박싱이 이루어진 `data`에 새로운 값 `200`을 넣은 것이기 때문에 `score`는 바뀌지 않는다.

### ToString()과의 혼동
기본 값 타입에서 `ToString()`을 호출하는 것 자체는 박싱과 혼동하기 쉽다.
```csharp
int score = 100;
string text = score.ToString();
```
- 이 코드는 `int`를 `string`으로 바꾸는 것이지, `object`에 넣는 박싱 예제가 아니다.

---
## C# - 언박싱(Unboxing)
언박싱은 `object`안에 **박싱되어 있는 값을 원래 값 타입으로 꺼내는 과정**이다.  
[object](./키워드%20정리.md#object)

### 언박싱 특징
1. 박싱은 자동으로 되지만 **언박싱은 자동으로 되는게 아닌 타입을 직접 캐스팅 해야한다.**
1. 언박싱을 할때는 **박싱된 값과 캐스팅 하려는 타입을 정확히 같게 해야한다.**
1. 명시적 **캐스팅이 반드시 필요**하다.

### 언박싱 예시
```csharp
int score = 100;
object data = score; // 박싱

int result = (int)data; // 여기서 언박싱이 일어난다.
```
- `data`가 `int`를 박싱한 `object`인지 확인한다.
- 맞으면 안의 값을 `int result`에 복사한다.
- 그러므로 **명시적 캐스팅 `(int)`가 반드시 필요하다.**

### 언박싱 오류 예시
```csharp
float result = (float)data; // InvalidCastException
```
- 박싱된 값 타입이 `int`였지만 캐스팅 하려는 타입이 `float`이라서 오류가 일어난다.

---

## C# - struct
`struct`는 **C#에서 작은 데이터를 값 자체로 표현할 떄 쓰는 타입**이다.  
Unity로 치면 `Vector3` / `Color` / `Quaternion`처럼 값 자체가 중요한 데이터에 잘 어울린다.  
기본적으로 **값 형식**이다.

### struct 특징
1. 구조체(`struct`)에는 필드, 프로퍼티, 생성자, 메서드, 연산자 등을 넣을 수 있다.
1. 클래스 처럼 데이터를 가지고 **기능을 정의**할 수 있다.
1. **값 형식**이라서 별개의 복사본(**값 복사**)가 가능하다.
1. 구조체는 **다른 클래스나 구조체를 상속할 수 없지만**, **인터페이스 구현은 가능**하다.

### struct 예시
```csharp
public struct PlayerStats
{
    public int hp;
    public int attack;
    public float moveSpeed;

    public PlayerStats(int hp, int attack, float moveSpeed)
    {
        this.hp = hp;
        this.attack = attack;
        this.moveSpeed = moveSpeed;
    }

    public void PrintInfo()
    {
        Debug.Log($"HP: {hp}, 공격력: {attack}, 이동 속도: {moveSpeed}");
    }
}
```
```csharp
PlayerStats warriorStats = new PlayerStats(100, 20, 5.0f);

warriorStats.PrintInfo();
// HP: 100, 공격력: 20, 이동 속도: 5
```
- 메서드 / 연산자 등을 복사 하였다.
- `this`는 `PlayerStats` 구조체 인스턴스인 `warriorStats`를 가리킨다.

### struct 값 복사 예시
```csharp
PlayerStats statsA = new PlayerStats(100, 20, 5.0f);
PlayerStats statsB = statsA;

statsB.hp = 50;

Debug.Log(statsA.hp); // 100
Debug.Log(statsB.hp); // 50
```
- `statsA` 에 `hp`에 `100`을 대입한다.
- `statsB = statsA`를 하며 `statsA`의 값이 `statsB`로 복사된다.
- 복사한 후 `statsB.hp = 50;`로 `statsB.hp`에 `50`을 대입한다.
- `Debug.Log(statsB.hp);`의 출력값이 `50`이 나오므로, 
같은 객체를 쓰는 데이터가 아닌 각각 독립적으로 존재하는 값 타입임을 알 수 있다.

### struct의 인터페이스 구현 예시
```csharp
public interface IDamageable
{
    void TakeDamage(int damage);
}

public struct DummyTarget : IDamageable
{
    public int hp;

    public DummyTarget(int hp)
    {
        this.hp = hp;
    }

    public void TakeDamage(int damage)
    {
        hp -= damage;
    }
}
```
- 인터페이스 `IDamageable`의 `TakeDamage()`를 구조체 내에서 구현 하였다.

---
## C# - Class

클래스(`class`)는 **객체를 만들기 위한 설계도**이다.

게임에서 플레이어, 몬스터, 인벤토리, 아이템, UI, 게임 매니저처럼  
**데이터와 행동을 함께 가지며 계속 상태가 변하는 대상**을 표현할 때 주로 사용한다.

클래스는 **참조 형식(Reference Type)** 이다.  
따라서 클래스 변수를 다른 변수에 대입하면 객체 자체가 복사되는 것이 아니라, 같은 객체를 가리키는 참조가 복사된다.

---

### class 특징

1. 클래스는 **참조 형식**이다.
1. 클래스에는 필드, 프로퍼티, 생성자, 메서드, 이벤트 등을 넣을 수 있다.
1. 클래스 변수에는 객체 데이터 자체가 아니라, 객체를 가리키는 **참조값**이 저장된다.
1. 클래스 변수를 다른 변수에 대입하면 객체가 새로 복사되는 것이 아니라, 같은 객체를 함께 가리키게 된다.
1. 클래스는 `null` 값을 가질 수 있다.
1. 클래스는 다른 클래스를 상속할 수 있다.
1. 클래스는 인터페이스를 구현할 수 있다.
1. 클래스는 `virtual`, `override`를 사용해 다형성을 구현할 수 있다.
1. Unity의 `MonoBehaviour` 스크립트도 클래스이다.

---

### class 기본 예시

```csharp
public class Player
{
    public string name;
    public int hp;
    public int attack;

    public void Attack()
    {
        Debug.Log($"{name}이(가) 공격합니다.");
    }

    public void TakeDamage(int damage)
    {
        hp -= damage;

        Debug.Log($"{name}이(가) {damage}의 피해를 받았습니다.");
    }
}
```

```csharp
Player player = new Player();

player.name = "용사";
player.hp = 100;
player.attack = 20;

player.Attack();
player.TakeDamage(30);

Debug.Log(player.hp); // 70
```

- `Player`는 플레이어 객체를 만들기 위한 클래스, 즉 설계도이다.
- `new Player()`를 사용하면 `Player` 설계도를 바탕으로 실제 객체가 생성된다.
- `name`, `hp`, `attack`은 플레이어가 가지는 데이터인 필드이다.
- `Attack()`, `TakeDamage()`는 플레이어가 수행할 수 있는 행동인 메서드이다.
- `player` 변수에는 생성된 `Player` 객체를 가리키는 참조값이 저장된다.

---

### class 참조 복사 예시

```csharp
public class Player
{
    public string name;
    public int hp;
}
```

```csharp
Player playerA = new Player();

playerA.name = "기사";
playerA.hp = 100;

Player playerB = playerA;

playerB.hp = 50;

Debug.Log(playerA.hp); // 50
Debug.Log(playerB.hp); // 50
```

- `playerA`는 `Player` 객체 하나를 생성하고 가리킨다.
- `playerB = playerA;`를 하면 플레이어 데이터가 복사되는 것이 아니다.
- `playerA`가 가리키던 같은 `Player` 객체를 `playerB`도 가리키게 된다.
- 따라서 `playerB.hp = 50;`으로 값을 바꾸면 같은 객체를 보고 있는 `playerA.hp`도 `50`으로 확인된다.
- 클래스는 구조체처럼 값 자체가 복사되는 것이 아니라, 객체의 참조가 복사되는 참조 형식이다.

```text
playerA ─┐
         ├── Player 객체 { name = "기사", hp = 50 }
playerB ─┘
```

---

### struct와 class 차이

| 구분 | struct | class |
|---|---|---|
| 형식 | 값 형식 | 참조 형식 |
| 대입 시 | 값 전체가 복사된다 | 객체를 가리키는 참조가 복사된다 |
| 변수 수정 시 | 복사본만 변경된다 | 같은 객체를 공유하면 모두 변경된다 |
| `null` 사용 | 기본적으로 불가능 | 가능 |
| 상속 | 불가능 | 가능 |
| 사용 예시 | `Vector3`, `Color`, 작은 스탯 | `Player`, `Enemy`, `GameManager` |

```csharp
// struct는 값이 복사된다.
PlayerStats statsA = new PlayerStats(100, 20, 5.0f);
PlayerStats statsB = statsA;

statsB.hp = 50;

Debug.Log(statsA.hp); // 100
Debug.Log(statsB.hp); // 50
```

```csharp
// class는 같은 객체를 가리킨다.
Player playerA = new Player();
playerA.hp = 100;

Player playerB = playerA;

playerB.hp = 50;

Debug.Log(playerA.hp); // 50
Debug.Log(playerB.hp); // 50
```

---

### class 생성자 예시

생성자(Constructor)는 `new`를 사용해 객체를 생성할 때 자동으로 실행되는 특별한 메서드이다.

객체를 만들 때 필요한 값을 한 번에 넣고, 객체의 초기 상태를 보장하기 위해 사용한다.

```csharp
public class Player
{
    public string name;
    public int hp;
    public int attack;

    public Player(string name, int hp, int attack)
    {
        this.name = name;
        this.hp = hp;
        this.attack = attack;
    }
}
```

```csharp
Player player = new Player("용사", 100, 20);

Debug.Log(player.name);   // 용사
Debug.Log(player.hp);     // 100
Debug.Log(player.attack); // 20
```

- `Player(string name, int hp, int attack)`는 `Player` 클래스의 생성자이다.
- 생성자 이름은 클래스 이름과 같아야 한다.
- `new Player("용사", 100, 20)`을 실행하면 생성자가 자동으로 호출된다.
- `this.name`은 현재 `Player` 객체의 필드이다.
- 오른쪽의 `name`은 생성자로 전달받은 매개변수이다.
- `this`는 현재 객체 자신을 의미한다.

---

### class 캡슐화 예시

필드를 모두 `public`으로 만들면 외부 코드에서 잘못된 값을 자유롭게 넣을 수 있다.

```csharp
public class Player
{
    public int hp;
}
```

```csharp
Player player = new Player();

player.hp = -9999;
```

이런 문제를 막기 위해 보통 필드는 `private`으로 숨기고,  
외부에는 필요한 기능만 `public` 메서드나 프로퍼티로 제공한다.

```csharp
public class Player
{
    private int hp;
    private int maxHp;

    public int Hp
    {
        get
        {
            return hp;
        }
    }

    public int MaxHp
    {
        get
        {
            return maxHp;
        }
    }

    public Player(int maxHp)
    {
        this.maxHp = maxHp;
        hp = maxHp;
    }

    public void TakeDamage(int damage)
    {
        if (damage < 0)
        {
            return;
        }

        hp -= damage;

        if (hp < 0)
        {
            hp = 0;
        }
    }

    public void Heal(int amount)
    {
        if (amount < 0)
        {
            return;
        }

        hp += amount;

        if (hp > maxHp)
        {
            hp = maxHp;
        }
    }
}
```

```csharp
Player player = new Player(100);

player.TakeDamage(30);

Debug.Log(player.Hp); // 70

player.Heal(50);

Debug.Log(player.Hp); // 100

// player.hp = -1;
// 오류: hp는 private 필드이므로 외부에서 직접 접근할 수 없다.
```

- `private`은 같은 클래스 내부에서만 접근할 수 있는 접근 제한자이다.
- `public`은 외부 클래스에서도 접근할 수 있는 접근 제한자이다.
- `Hp`는 값을 읽을 수만 있는 프로퍼티이다.
- 외부에서는 `TakeDamage()`, `Heal()` 메서드를 통해서만 HP를 변경할 수 있다.
- 이처럼 데이터를 숨기고, 정해진 기능을 통해서만 값을 바꾸게 하는 방식을 **캡슐화**라고 한다.

---

### 자동 프로퍼티 예시

간단히 값을 읽게 하거나, 외부에서 변경하지 못하게 할 때 자동 프로퍼티를 사용할 수 있다.

```csharp
public class Item
{
    public string Name { get; private set; }
    public int Price { get; private set; }

    public Item(string name, int price)
    {
        Name = name;
        Price = price;
    }
}
```

```csharp
Item potion = new Item("체력 포션", 100);

Debug.Log(potion.Name);  // 체력 포션
Debug.Log(potion.Price); // 100

// potion.Price = 1;
// 오류: Price의 set은 private이므로 외부에서 변경할 수 없다.
```

- `get`은 값을 가져오는 기능이다.
- `set`은 값을 변경하는 기능이다.
- `get; private set;`은 외부에서 값을 읽는 것은 가능하지만, 변경은 클래스 내부에서만 가능하다는 뜻이다.

---

### class 메서드 전달 예시

클래스는 참조 형식이므로 메서드 안에서 객체의 필드를 수정하면 원본 객체도 바뀐다.

```csharp
public class Player
{
    public int hp;
}
```

```csharp
public void TakeDamage(Player player, int damage)
{
    player.hp -= damage;
}

Player player = new Player();
player.hp = 100;

TakeDamage(player, 20);

Debug.Log(player.hp); // 80
```

- `TakeDamage()` 메서드가 받은 `player`는 원본 객체를 가리키는 참조값의 복사본이다.
- 참조값은 복사되었지만, 복사본도 결국 같은 `Player` 객체를 가리킨다.
- 따라서 메서드 내부에서 `player.hp`를 변경하면 원본 객체의 HP도 변경된다.

하지만 메서드 내부에서 매개변수에 새로운 객체를 넣어도 호출한 쪽 변수는 바뀌지 않는다.

```csharp
public void ReplacePlayer(Player player)
{
    player = new Player();
    player.hp = 999;
}

Player player = new Player();
player.hp = 100;

ReplacePlayer(player);

Debug.Log(player.hp); // 100
```

- 메서드 안의 `player`는 참조값의 복사본이다.
- `player = new Player();`는 메서드 내부의 복사본 참조만 새 객체로 바꾼다.
- 호출한 쪽의 `player` 변수는 여전히 기존 객체를 가리킨다.

호출한 쪽 변수 자체가 새 객체를 가리키게 만들고 싶다면 `ref`를 사용해야 한다.

```csharp
public void ReplacePlayer(ref Player player)
{
    player = new Player();
    player.hp = 999;
}

Player player = new Player();
player.hp = 100;

ReplacePlayer(ref player);

Debug.Log(player.hp); // 999
```

---

### class null 예시

클래스 변수는 객체를 가리키지 않는 `null` 상태가 될 수 있다.

```csharp
Player player = null;
```

`null`인 상태에서 객체의 필드나 메서드에 접근하면 오류가 발생한다.

```csharp
Player player = null;

// Debug.Log(player.hp);
// NullReferenceException 오류 발생
```

따라서 Unity에서 인스펙터 참조나 `GetComponent()` 결과를 사용할 때는 널 검사가 중요하다.

```csharp
[SerializeField] private PlayerController player;

private void Start()
{
    if (player == null)
    {
        Debug.LogError("PlayerController가 연결되지 않았습니다.");
        return;
    }

    player.TakeDamage(10);
}
```

- `player == null`은 `player`가 아무 객체도 가리키지 않는지 확인하는 조건이다.
- 참조가 연결되지 않았을 때 실행을 멈추면 `NullReferenceException`을 예방할 수 있다.

---

### class 상속 예시

상속은 부모 클래스의 데이터와 기능을 자식 클래스가 물려받는 기능이다.

```csharp
public class Character
{
    public string Name { get; protected set; }
    public int Hp { get; protected set; }

    public Character(string name, int hp)
    {
        Name = name;
        Hp = hp;
    }

    public void TakeDamage(int damage)
    {
        Hp -= damage;

        if (Hp < 0)
        {
            Hp = 0;
        }

        Debug.Log($"{Name}의 현재 HP: {Hp}");
    }
}
```

```csharp
public class Player : Character
{
    public int Level { get; private set; }

    public Player(string name, int hp, int level)
        : base(name, hp)
    {
        Level = level;
    }

    public void GainExperience()
    {
        Debug.Log($"{Name}이(가) 경험치를 획득했습니다.");
    }
}
```

```csharp
public class Enemy : Character
{
    public int RewardGold { get; private set; }

    public Enemy(string name, int hp, int rewardGold)
        : base(name, hp)
    {
        RewardGold = rewardGold;
    }

    public void DropGold()
    {
        Debug.Log($"{Name}이(가) {RewardGold} 골드를 떨어뜨렸습니다.");
    }
}
```

```csharp
Player player = new Player("용사", 100, 1);
Enemy slime = new Enemy("슬라임", 30, 10);

player.TakeDamage(20);
player.GainExperience();

slime.TakeDamage(30);
slime.DropGold();
```

- `Character`는 플레이어와 적이 공통으로 가지는 이름, HP, 피해 처리 기능을 담은 부모 클래스이다.
- `Player : Character`는 `Player`가 `Character`를 상속한다는 뜻이다.
- `Enemy : Character`도 같은 방식으로 `Character`를 상속한다.
- `protected`는 현재 클래스와 이 클래스를 상속받은 자식 클래스에서 접근할 수 있다.
- `base(name, hp)`는 부모 클래스인 `Character`의 생성자를 호출한다.
- 공통 기능을 부모 클래스에 작성하면 중복 코드를 줄일 수 있다.

---

### virtual과 override 예시

`virtual`과 `override`를 사용하면 부모 클래스의 메서드를 자식 클래스마다 다르게 구현할 수 있다.

```csharp
public class Enemy
{
    public string Name { get; protected set; }

    public Enemy(string name)
    {
        Name = name;
    }

    public virtual void Attack()
    {
        Debug.Log($"{Name}이(가) 기본 공격을 합니다.");
    }
}
```

```csharp
public class Slime : Enemy
{
    public Slime(string name) : base(name)
    {
    }

    public override void Attack()
    {
        Debug.Log($"{Name}이(가) 몸통 박치기를 합니다.");
    }
}
```

```csharp
public class Goblin : Enemy
{
    public Goblin(string name) : base(name)
    {
    }

    public override void Attack()
    {
        Debug.Log($"{Name}이(가) 단검을 휘두릅니다.");
    }
}
```

```csharp
Enemy slime = new Slime("슬라임");
Enemy goblin = new Goblin("고블린");

slime.Attack();  // 슬라임이 몸통 박치기를 합니다.
goblin.Attack(); // 고블린이 단검을 휘두릅니다.
```

- 부모 클래스의 `Attack()` 메서드에 `virtual`을 붙인다.
- 자식 클래스는 `override`를 사용해 부모의 `Attack()` 메서드를 재정의한다.
- 변수 타입은 모두 `Enemy`이지만, 실제로 생성된 객체가 `Slime`인지 `Goblin`인지에 따라 실행되는 공격이 달라진다.
- 이렇게 부모 타입으로 여러 객체를 관리하면서, 실제 객체마다 다른 행동을 실행하는 것을 **다형성**이라고 한다.

```csharp
List<Enemy> enemies = new List<Enemy>()
{
    new Slime("슬라임"),
    new Goblin("고블린"),
    new Slime("파란 슬라임")
};

foreach (Enemy enemy in enemies)
{
    enemy.Attack();
}
```

```text
슬라임이 몸통 박치기를 합니다.
고블린이 단검을 휘두릅니다.
파란 슬라임이 몸통 박치기를 합니다.
```

---

### abstract class 예시

`abstract class`는 공통 기능과 규칙을 제공하는 부모 설계도이다.

추상 클래스는 직접 객체를 만들 수 없고, 상속받은 자식 클래스가 필요한 기능을 구현하도록 강제할 수 있다.

[추상 클래스](./키워드%20정리.md#추상클래스)

```csharp
public abstract class Skill
{
    public string Name { get; protected set; }
    public int ManaCost { get; protected set; }

    protected Skill(string name, int manaCost)
    {
        Name = name;
        ManaCost = manaCost;
    }

    public abstract void Use();
}
```

```csharp
public class FireballSkill : Skill
{
    public FireballSkill()
        : base("파이어볼", 20)
    {
    }

    public override void Use()
    {
        Debug.Log($"{Name} 사용! 마나 {ManaCost}를 소모합니다.");
    }
}
```

```csharp
public class HealSkill : Skill
{
    public HealSkill()
        : base("힐", 15)
    {
    }

    public override void Use()
    {
        Debug.Log($"{Name} 사용! 체력을 회복합니다.");
    }
}
```

```csharp
List<Skill> skills = new List<Skill>()
{
    new FireballSkill(),
    new HealSkill()
};

foreach (Skill skill in skills)
{
    skill.Use();
}
```

```csharp
// Skill skill = new Skill();
// 오류: abstract class는 직접 new로 생성할 수 없다.
```

- `Skill`은 모든 스킬이 공통으로 가져야 하는 이름, 마나 비용, 사용 기능을 정의한다.
- `abstract void Use();`는 자식 클래스가 반드시 구현해야 하는 메서드이다.
- `FireballSkill`, `HealSkill`은 각각 자신만의 `Use()` 기능을 구현한다.

---

### static class와 static 멤버 예시

`static` 멤버는 객체마다 따로 생성되지 않고 클래스 전체가 공유한다.

```csharp
public class Player
{
    public static int PlayerCount = 0;

    public string Name { get; private set; }

    public Player(string name)
    {
        Name = name;
        PlayerCount++;
    }
}
```

```csharp
Player player1 = new Player("용사");
Player player2 = new Player("마법사");

Debug.Log(Player.PlayerCount); // 2
```

- `PlayerCount`는 각 플레이어가 따로 가지는 값이 아니다.
- 모든 `Player` 객체가 공유하는 하나의 값이다.
- `static` 멤버는 보통 `클래스이름.멤버이름`으로 접근한다.

`static class`는 객체를 만들지 않고 공용 기능을 제공할 때 사용한다.

```csharp
public static class DamageCalculator
{
    public static int CalculateDamage(int attack, int defense)
    {
        int damage = attack - defense;

        if (damage < 1)
        {
            damage = 1;
        }

        return damage;
    }
}
```

```csharp
int damage = DamageCalculator.CalculateDamage(20, 8);

Debug.Log(damage); // 12
```

```csharp
// DamageCalculator calculator = new DamageCalculator();
// 오류: static class는 new로 객체를 생성할 수 없다.
```

- `DamageCalculator`는 특정 객체의 상태를 저장할 필요가 없는 계산용 클래스이다.
- 이런 공용 계산 기능은 `static class`로 만드는 것이 적합하다.

---

### Unity MonoBehaviour 예시

Unity에서 작성하는 컴포넌트 스크립트도 클래스이다.

```csharp
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    [SerializeField] private float moveSpeed = 5.0f;

    private int hp = 100;

    private void Update()
    {
        Move();
    }

    private void Move()
    {
        float horizontal = Input.GetAxisRaw("Horizontal");
        float vertical = Input.GetAxisRaw("Vertical");

        Vector3 direction = new Vector3(horizontal, 0, vertical).normalized;

        transform.position += direction * moveSpeed * Time.deltaTime;
    }

    public void TakeDamage(int damage)
    {
        hp -= damage;

        if (hp <= 0)
        {
            hp = 0;
            Debug.Log("플레이어 사망");
        }
    }
}
```

- `PlayerController`는 클래스이다.
- `: MonoBehaviour`는 `PlayerController`가 Unity의 `MonoBehaviour` 클래스를 상속한다는 뜻이다.
- `[SerializeField]`를 사용하면 `private` 필드도 Unity Inspector에서 설정할 수 있다.
- `Update()`는 Unity가 매 프레임 호출하는 메서드이다.
- `transform`은 `MonoBehaviour`를 상속받은 컴포넌트에서 사용할 수 있는 Transform 참조이다.
- `MonoBehaviour`를 상속한 스크립트는 일반 클래스와 다르게 `new PlayerController()`로 직접 생성하면 안 된다.
- Unity에서 GameObject에 컴포넌트로 붙이거나 `AddComponent<PlayerController>()`를 사용해야 한다.

---

## C# - 매개변수 한정자(ref)

`ref`는 메서드에 변수를 전달할 때, 값의 복사본이 아닌 **원본 변수 자체를 참조로 전달**하는 매개변수 한정자이다.

기본적으로 메서드에 값을 전달하면 값이 복사된다.  
하지만 `ref`를 사용하면 메서드 내부에서 원본 변수를 직접 읽고 수정할 수 있다.

---

### ref 특징

1. `ref`는 변수를 **참조로 전달**한다.
1. 메서드 내부에서 값을 변경하면 호출한 쪽의 원본 변수도 변경된다.
1. `ref`는 메서드 선언부와 호출부에 모두 작성해야 한다.
1. `ref`로 전달할 변수는 호출 전에 반드시 초기화되어 있어야 한다.
1. `ref`는 기존 값을 읽고 수정할 수 있다.
1. `ref`에는 리터럴 값, 계산 결과, `new`로 만든 임시 값 등을 직접 전달할 수 없다.
1. `struct`는 기본적으로 값이 복사되므로, 원본 구조체를 변경하려면 `ref`를 사용할 수 있다.
1. `class`는 `ref` 없이도 객체 내부 데이터를 수정할 수 있다.
1. 하지만 클래스 변수가 가리키는 객체 자체를 새 객체로 교체하려면 `ref`가 필요하다.

---

### ref 기본 예시

```csharp
public void AddTen(ref int number)
{
    number += 10;
}
```

```csharp
int score = 100;

AddTen(ref score);

Debug.Log(score); // 110
```

- `score`는 `100`으로 초기화된 변수이다.
- `AddTen(ref score)`를 호출하면 `score`의 복사본이 전달되는 것이 아니다.
- 메서드 매개변수 `number`가 호출부의 `score` 원본 변수를 직접 참조한다.
- 따라서 `number += 10;`은 `score += 10;`과 같은 결과를 만든다.
- 메서드 호출이 끝난 뒤 `score`의 값은 `110`이 된다.

```text
ref 사용 전

score → 100

ref 사용 후

score ─┐
       ├── 110
number ─┘
```

---

### ref를 사용하지 않은 값 복사 예시

기본 매개변수 전달은 값 복사 방식이다.

```csharp
public void AddTen(int number)
{
    number += 10;
}
```

```csharp
int score = 100;

AddTen(score);

Debug.Log(score); // 100
```

- `AddTen(score)`를 호출하면 `score`의 값인 `100`이 `number`에 복사된다.
- 메서드 내부의 `number`는 `score`와 별개의 변수이다.
- 따라서 메서드 안에서 `number`를 `110`으로 바꿔도 원본 `score`는 `100`으로 유지된다.

```text
메서드 호출 전

score = 100

메서드 호출 시

score  = 100
number = 100  // score 값의 복사본

메서드 내부

number = 110

메서드 종료 후

score = 100
```

---

### ref 사용 규칙

`ref`는 메서드 선언부와 호출부에 모두 작성해야 한다.

```csharp
public void AddTen(ref int number)
{
    number += 10;
}

int score = 100;

AddTen(ref score);
```

다음처럼 선언부 또는 호출부 한쪽에만 `ref`를 쓰면 오류가 발생한다.

```csharp
public void AddTen(ref int number)
{
    number += 10;
}

// AddTen(score);
// 오류: 호출부에도 ref를 작성해야 한다.
```

```csharp
public void AddTen(int number)
{
    number += 10;
}

// AddTen(ref score);
// 오류: 선언부에도 ref를 작성해야 한다.
```

---

### ref 초기화 규칙

`ref`로 전달하는 변수는 반드시 미리 초기화되어 있어야 한다.

```csharp
public void AddTen(ref int number)
{
    number += 10;
}
```

```csharp
int score;

// AddTen(ref score);
// 오류: score가 초기화되지 않았다.
```

```csharp
int score = 0;

AddTen(ref score);

Debug.Log(score); // 10
```

- `ref` 매개변수는 메서드 안에서 기존 값을 읽을 수 있다.
- 따라서 호출 전 원본 변수에 값이 있어야 한다.
- `out`은 기존 값을 읽을 필요가 없고, 메서드가 값을 만들어 넣는 방식이라 초기화 규칙이 다르다.

---

### ref 전달 불가능한 경우

`ref`는 원본 변수의 위치를 직접 참조해야 하므로, 실제 변수만 전달할 수 있다.

```csharp
public void AddTen(ref int number)
{
    number += 10;
}
```

```csharp
// AddTen(ref 100);
// 오류: 숫자 리터럴은 변수가 아니다.

// AddTen(ref (10 + 20));
// 오류: 계산 결과는 별도의 변수가 아니다.

// AddTen(ref new int());
// 오류: 임시로 생성한 값은 원본 변수로 전달할 수 없다.
```

다음처럼 값을 저장하는 변수를 만든 뒤 전달해야 한다.

```csharp
int score = 100;

AddTen(ref score);

Debug.Log(score); // 110
```

---

### struct와 ref

`struct`는 값 형식이므로, 일반적으로 메서드에 전달하면 구조체 값 전체가 복사된다.

```csharp
public struct PlayerStats
{
    public int hp;
    public int attack;

    public PlayerStats(int hp, int attack)
    {
        this.hp = hp;
        this.attack = attack;
    }
}
```

```csharp
public void TakeDamage(PlayerStats stats, int damage)
{
    stats.hp -= damage;
}
```

```csharp
PlayerStats warriorStats = new PlayerStats(100, 20);

TakeDamage(warriorStats, 30);

Debug.Log(warriorStats.hp); // 100
```

- `warriorStats`는 `hp`가 `100`인 구조체이다.
- `TakeDamage(warriorStats, 30)`을 호출하면 `warriorStats`의 값이 `stats`에 복사된다.
- 메서드 안에서 `stats.hp`는 `70`이 되지만, 이것은 복사본의 값이다.
- 원본인 `warriorStats.hp`는 `100`으로 유지된다.

```text
메서드 호출 전

warriorStats → { hp: 100, attack: 20 }

TakeDamage 호출 시

stats → { hp: 100, attack: 20 }  // 복사본

메서드 내부

stats.hp = 70

메서드 종료 후

warriorStats → { hp: 100, attack: 20 }
```

원본 구조체를 직접 변경하려면 `ref`를 사용한다.

```csharp
public void TakeDamage(ref PlayerStats stats, int damage)
{
    stats.hp -= damage;

    if (stats.hp < 0)
    {
        stats.hp = 0;
    }
}
```

```csharp
PlayerStats warriorStats = new PlayerStats(100, 20);

TakeDamage(ref warriorStats, 30);

Debug.Log(warriorStats.hp); // 70
```

- `ref warriorStats`를 전달하면 구조체 전체가 복사되지 않는다.
- 메서드의 `stats`는 호출부의 `warriorStats` 원본 변수를 직접 참조한다.
- 따라서 `stats.hp`를 변경하면 `warriorStats.hp`도 함께 변경된다.

```text
warriorStats ─┐
              ├── { hp: 70, attack: 20 }
stats ────────┘
```

---

### Vector3와 ref 예시

Unity의 `Vector3`도 구조체이다.

일반 매개변수로 전달하면 `Vector3` 값이 복사된다.

```csharp
public void MoveRight(Vector3 position)
{
    position.x += 1.0f;
}
```

```csharp
Vector3 playerPosition = new Vector3(0, 0, 0);

MoveRight(playerPosition);

Debug.Log(playerPosition.x); // 0
```

- `playerPosition`의 값이 `position`에 복사된다.
- 메서드 안에서 `position.x`는 `1`이 되지만, 원본 `playerPosition.x`는 `0`이다.

원본 `Vector3` 변수를 직접 변경하려면 `ref`를 사용한다.

```csharp
public void MoveRight(ref Vector3 position)
{
    position.x += 1.0f;
}
```

```csharp
Vector3 playerPosition = new Vector3(0, 0, 0);

MoveRight(ref playerPosition);

Debug.Log(playerPosition.x); // 1
```

- 메서드의 `position`은 `playerPosition` 원본 변수를 참조한다.
- 따라서 `position.x += 1.0f;`의 결과가 원본 `playerPosition`에도 반영된다.

---

### transform.position과 ref

`transform.position`은 `Vector3` 구조체이지만, 일반 변수나 필드가 아닌 **프로퍼티**이다.

따라서 다음처럼 직접 `ref` 인수로 전달할 수 없다.

```csharp
// MoveRight(ref transform.position);
// 오류: 프로퍼티는 ref 인수로 직접 전달할 수 없다.
```

프로퍼티의 값을 지역 변수에 복사하고, 수정한 뒤 다시 대입해야 한다.

```csharp
Vector3 position = transform.position;

MoveRight(ref position);

transform.position = position;
```

- `transform.position`의 값을 지역 변수 `position`에 저장한다.
- `position`은 실제 지역 변수이므로 `ref`로 전달할 수 있다.
- 수정이 끝난 뒤 `transform.position = position;`으로 변경된 위치를 다시 적용한다.

---

### class와 ref

클래스는 참조 형식이다.

클래스 변수를 일반 매개변수로 전달하면 객체 자체가 복사되는 것이 아니라, 객체를 가리키는 참조값이 복사된다.

따라서 `ref` 없이도 같은 객체의 내부 필드를 수정할 수 있다.

```csharp
public class Player
{
    public string name;
    public int hp;

    public Player(string name, int hp)
    {
        this.name = name;
        this.hp = hp;
    }
}
```

```csharp
public void TakeDamage(Player player, int damage)
{
    player.hp -= damage;
}
```

```csharp
Player player = new Player("용사", 100);

TakeDamage(player, 30);

Debug.Log(player.hp); // 70
```

- 호출부의 `player`와 메서드 매개변수 `player`는 서로 다른 변수이다.
- 하지만 두 변수 안에는 같은 `Player` 객체를 가리키는 참조값이 들어 있다.
- 메서드에서 `player.hp`를 수정하면 같은 객체 내부의 HP가 변경된다.
- 호출부에서도 같은 객체를 보고 있으므로 `70`이 출력된다.

```text
호출부 player ───────→ Player 객체 { hp: 70 }

메서드 player ───────→ 같은 Player 객체
```

---

### class에서 ref 없이 새 객체 교체

다음 코드는 메서드 내부의 매개변수만 새 객체를 가리키게 한다.

```csharp
public void Respawn(Player player)
{
    player = new Player("용사", 100);
}
```

```csharp
Player player = new Player("용사", 0);

Respawn(player);

Debug.Log(player.hp); // 0
```

- 호출부에서 HP가 `0`인 Player 객체 A를 생성한다.
- `Respawn(player)`를 호출하면, 객체 A를 가리키는 참조값이 메서드 매개변수 `player`에 복사된다.
- 메서드 시작 시 호출부와 메서드의 `player`는 모두 객체 A를 가리킨다.
- 메서드 안의 `new Player("용사", 100)`은 HP가 `100`인 새로운 객체 B를 생성한다.
- `player = new Player("용사", 100);`은 메서드 매개변수 `player`만 객체 B를 가리키도록 변경한다.
- 호출부 `player`는 계속 객체 A를 가리키므로 HP는 `0`이다.

```text
Respawn 호출 전

호출부 player ───→ 객체 A { hp: 0 }

Respawn 호출 중

호출부 player ───→ 객체 A { hp: 0 }
메서드 player ───→ 객체 A { hp: 0 }

player = new Player("용사", 100) 실행 후

호출부 player ───→ 객체 A { hp: 0 }
메서드 player ───→ 객체 B { hp: 100 }
```

---

### class에서 ref로 새 객체 교체

호출부 변수 자체가 새 객체를 가리키게 만들려면 `ref`를 사용한다.

```csharp
public void Respawn(ref Player player)
{
    player = new Player("용사", 100);
}
```

```csharp
Player player = new Player("용사", 0);

Respawn(ref player);

Debug.Log(player.hp); // 100
```

- `ref player`를 전달하면 메서드는 호출부의 `player` 변수 자체를 직접 참조한다.
- 메서드 안에서 새 `Player` 객체를 대입하면 호출부의 `player`도 새 객체를 가리키게 된다.
- 따라서 `Debug.Log(player.hp)`의 결과는 `100`이다.

```text
Respawn 전

player ───→ 객체 A { hp: 0 }

Respawn 후

player ───→ 객체 B { hp: 100 }
```

---

### ref와 out 차이

`ref`와 `out`은 모두 참조 전달 방식이지만 목적이 다르다.

| 구분 | ref | out |
|---|---|---|
| 호출 전 초기화 | 반드시 필요 | 필요 없음 |
| 메서드에서 기존 값 읽기 | 가능 | 값을 넣기 전에는 불가능 |
| 메서드에서 값 대입 | 선택 | 반드시 해야 함 |
| 주 용도 | 기존 원본 값을 수정 | 메서드 결과를 추가로 반환 |
| 호출 문법 | `Method(ref value)` | `Method(out value)` |

```csharp
public void Increase(ref int value)
{
    value += 10;
}
```

```csharp
public void CreateScore(out int score)
{
    score = 100;
}
```

```csharp
int score = 50;

Increase(ref score);

Debug.Log(score); // 60

int newScore;

CreateScore(out newScore);

Debug.Log(newScore); // 100
```

- `ref`는 이미 존재하는 값을 읽고 수정하는 데 사용한다.
- `out`은 메서드가 새로운 결과를 만들어 호출부 변수에 대입하는 데 사용한다.
- `out` 매개변수는 메서드가 끝나기 전에 반드시 값을 대입해야 한다.

---

### ref와 in 차이

`in`도 참조로 전달하지만 읽기 전용이다.

| 구분 | ref | in |
|---|---|---|
| 전달 방식 | 참조 전달 | 읽기 전용 참조 전달 |
| 메서드 내부 원본 수정 | 가능 | 불가능 |
| 기존 값 읽기 | 가능 | 가능 |
| 호출 전 초기화 | 필요 | 필요 |
| 주 용도 | 원본 값 변경 | 큰 구조체를 복사 없이 읽기 |

```csharp
public void AddDamage(ref PlayerStats stats)
{
    stats.attack += 10;
}
```

```csharp
public void PrintStats(in PlayerStats stats)
{
    Debug.Log(stats.hp);

    // stats.hp = 0;
    // 오류: in 매개변수는 수정할 수 없다.
}
```

- `ref`는 원본을 읽고 수정할 수 있다.
- `in`은 원본을 읽을 수 있지만 수정할 수 없다.
- 큰 구조체를 복사하지 않고 읽기만 해야 할 때 `in`을 사용할 수 있다.

---















    






